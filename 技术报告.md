# 基于人工智能的阵列天线综合技术研究报告

> 题目编号：XA-202604
> 发榜单位：中国电子科技集团公司第二十九研究所

---

## 摘要

本研究针对大规模阵列天线方向图综合问题，提出了一套基于解析基线与 AI 残差补偿相结合的综合方法。在 32×32（1024 阵元）均匀平面阵上，Taylor 可分离激励 + LCMV 自适应置零实现了全空域和波束副瓣 ≤ -35 dBc、差波束零深 ≤ -38 dB、4 点自适应置零 ≤ -38 dB 的竞赛指标，73 个独立扫描方向 100% 达标。研究系统验证了阵元失效补偿和轻度非均匀阵列在均匀平面条件下 SOCP 无法改善 Taylor 基线（负结果），明确了 AI 需在解析方法确实失效的场景才有增量价值。曲面阵列（抛物面 z = α(x²+y²)）验证表明，在 α ≥ 0.10 时 Taylor 退化 14-18 dB，SOCP 可稳定改善 4.2-4.6 dB，确认了曲面阵列综合是 AI 的有效方向。基于排列等变 DeepSets 网络的曲面阵列 AI 综合实现恢复率 82.4%-114.6%（AI 超越 SOCP 教师），NPU 推理 0.504ms（比 SOCP 快 45,635 倍），全流程在华为昇腾 910 NPU 上实现训练与部署。

**关键词**：阵列天线综合；Taylor 分布；LCMV 置零；SOCP；曲面阵列；DeepSets；Ascend NPU

---

## 一、国内外发展调研分析

### 1.1 传统阵列综合方法

天线阵列方向图综合是雷达与通信系统的核心技术。传统解析方法包括 Dolph-Chebyshev 法[1]、Taylor 法[2]和 Woodward-Lawson 法[3]，利用明确数学表达式将激励电流与方向图性能关联，计算效率突出。对于均匀平面阵，Taylor 可分离激励将二维问题分解为两个一维问题，具有闭式解且计算量极小。然而，这些方法仅适用于规则均匀阵列，对曲面阵列和非均匀阵列无闭式解。

基于数值优化的遗传算法（GA）[4]、粒子群优化（PSO）[5]和差分进化（DE）[6]提供更高设计自由度，但多轮迭代耗时长（典型 200 代需约 30 秒），难以满足实时性要求。交替投影法（AP）[7]可处理掩膜约束，但在大规模阵列上收敛不稳定。

### 1.2 深度学习方法

近年来，深度学习在天线综合领域取得显著进展。Cui C. 等[8]基于 MLP 结构设计深度神经网络用于直线阵列波束成型。Zhao Z. 等[9]提出二阶神经网络模型，从无耦合方向图中提取一般特征。Yang X. 等[10]采用改进损失函数实现无需数据集的稀疏阵列综合，与 PINN[11]理念一致。

Liu R. 等[12]提出基于 LSTM 层和编码器-解码器结构的循环神经网络，借鉴 NLP 领域经验处理可变长度天线阵列综合。该方法改进了"词嵌入"技术使网络能将实数映射为高维向量，在 15-30 阵元非均匀线阵上实现 45°-135° 主瓣覆盖，副瓣电平 15 dB。

现有深度学习方法的共同局限在于：大多基于 MLP 或 CNN 等顺序模型，只能处理固定尺寸输入输出，缺乏对可变长度阵列和任意坐标阵列的泛化能力。

### 1.3 SOCP 与凸优化

二阶锥规划（SOCP）可直接最小化最大副瓣（minimax），是判断权值是否已接近全局最优的最干净方法。对于 N 个阵元、M 个副瓣采样点的问题，SOCP 的变量数为 N（复权值）+ 1（最大副瓣水平 t），约束包括主瓣复响应、副瓣采样点幅度上界和零陷约束。CLARABEL 等内点法求解器可在 1-15 秒内求解 1024 阵元问题。

### 1.4 本研究切入点

本研究在已有工作基础上：
1. 修正原始 LSTM 代码 7 项关键 Bug（相位公式、单位、符号、accuracy 维度、loss mask、LCMV 尺度、索引类型）
2. 建立正确的统一评估器（uv 均匀网格 + -30dB 连通域主瓣检测）
3. 系统验证 AI 在均匀平面阵下的增量边界（负结果）
4. 确认曲面阵列是 AI 有明确价值的方向（正结果）
5. 全流程在 Ascend 910 NPU 上实现

---

## 二、研究内容和技术路线

### 2.1 总体方案

```
┌─────────────────────────────────────────────────────┐
│              输入：阵元坐标 + 波束参数               │
│         (θ₀, φ₀, SLL, 零陷方向, 失效mask)          │
└────────────────────┬────────────────────────────────┘
                     │
         ┌───────────┴───────────┐
         │                       │
    ┌────▼────┐            ┌────▼────┐
    │ 解析基线 │            │ AI 网络  │
    │Taylor + │            │DeepSets │
    │  LCMV   │            │  (曲面) │
    └────┬────┘            └────┬────┘
    ~1ms                     ~50ms
         │                       │
         └───────────┬───────────┘
                     │
              ┌──────▼──────┐
              │ 最终激励权值 │
              └─────────────┘
```

### 2.2 技术路线

| 阶段 | 核心内容 | 状态 |
|---|---|---|
| 1. 物理层 | PyTorch 可微方向图计算 + 修正 7 项原代码 Bug | ✅ 完成 |
| 2. 解析基线 | Taylor 可分离 + 3×3dB_BW 主瓣排除 + 60° 扫描诊断 | ✅ 完成 |
| 3. LCMV 置零 | 主瓣约束尺度修正 + 73 方向正式验收 | ✅ 完成 |
| 4. 非理想实验 | 量化/失效/扰动/频偏 + 固定激励 | ✅ 完成 |
| 5. AI 方向探索 | 失效补偿(负) + 非均匀坐标(负) + 曲面(正) | ✅ 完成 |
| 6. 曲面 AI | DeepSets 排列等变网络 + NPU 基准验证 | ✅ 完成 |

### 2.3 关键技术决策

1. **可分离分解**：32×32 阵列分解为 x/y 两个 1D 问题（32+32 自由度），避免网络规模失控
2. **3×3dB_BW 主瓣排除**：发现 2×3dB_BW 在大扫描角下太小（主瓣裙边误判为副瓣），3×3dB_BW 口径下 100% 达标
3. **LCMV 主瓣约束归一化**：先归一化 w_ref 使 a_main^H w_ref = 1，避免约束尺度不匹配导致 SLL 从 -35 暴跌到 -11 dB
4. **布尔索引安全**：使用 w[fmask] = 0 而非 w[~active_idx] = 0（整数按位取反导致负索引 Bug）
5. **SOCP 求解器**：CLARABEL 替代 SCS（SCS 在 1024 阵元大规模问题上不收敛）
6. **评估器**：uv 均匀网格 + -30dB 8 连通域主瓣检测 + conj(w)·exp(j·k·(x·u+y·v)) 相位约定

---

## 三、算法设计方法

### 3.1 物理层

#### 3.1.1 阵因子

一维线阵方向图：

$$F(\theta) = \sum_{n} I_n \cdot e^{j(k \cos\theta \cdot x_n - \varphi_n)}$$

二维平面阵方向图：

$$F(\theta,\varphi) = \sum_m \sum_n I_{mn} \cdot e^{j(k(\sin\theta\cos\varphi \cdot x_m + \sin\theta\sin\varphi \cdot y_n) - \varphi_{mn})}$$

曲面阵列（三维坐标）方向图：

$$F(\theta,\varphi) = \sum_n I_n \cdot e^{j(k(x_n u + y_n v + z_n w) - \varphi_n)}$$

其中 $u = \sin\theta\cos\varphi$, $v = \sin\theta\sin\varphi$, $w = \cos\theta$。

所有计算统一用弧度，相位符号为 $\psi = k \cdot x - \varphi$。

#### 3.1.2 原代码 Bug 修正

本研究在移植 Liu R. [12] 的 TensorFlow 代码到 PyTorch 时，发现并修正了 7 项关键 Bug：

| 编号 | Bug 描述 | 影响 | 修正 |
|---|---|---|---|
| 1 | 相位公式 `linspace(0,-cos(θ₀)(N-1),N)` 假设阵元从位置 0 开始，与中心对称坐标系不匹配 | 120° 波束指向错误为 60° | `k·cos(θ₀)·pos` |
| 2 | CreateDataset 中相位为度(0-360)，方向图计算中当弧度用 | 副瓣计算错误 | 统一弧度 |
| 3 | tf_calculate 中 `psi = k*x + phase`（应为减号） | 相位约定不一致 | 统一 `- phase` |
| 4 | accuracy 计算 `dim = y_true.shape[-1]` 在 reshape 后为 2（通道数）而非 32（词表大小） | 虚假准确率 97% → 真实 5% | `dim = y_true.shape[-2]` |
| 5 | `masked_mse_loss` 中 `.any(dim=-1)` 把所有非全零时间步标记为 padding | loss 恒为 0 | `.sum(dim=(2,3)) == 0` |
| 6 | LCMV `f[0] = 1.0` 与 Taylor 主瓣响应（~数百）尺度不匹配 | SLL 从 -35 暴跌到 -11 dB | 先归一化 `w_ref` 使 `a_main^H w_ref = 1` |
| 7 | `w_no[~active_idx] = 0` 对整数索引做按位取反，得到负索引 | 无补偿 SLL 从 -31 变成 -6 dB | `w_no[fmask] = 0`（布尔索引） |

#### 3.1.3 SLL 评估方法

本研究发现主瓣排除方法对评估结果有决定性影响：

- **2×3dB_BW/cos(θ₀)**：在大扫描角（60°）下排除区太小，主瓣裙边被误判为副瓣，SLL 虚假恶化至 -24 dB
- **3×3dB_BW/cos(θ₀)**：正确排除主瓣区域，SLL = -35.2 dB
- **方向自适应第一零点**：每个方位角独立找第一零点（非圆形），更严格但可能过严

最终采用 3×3dB_BW 作为竞赛标准口径，同时保留严格口径作为对比。

### 3.2 Taylor 可分离基线

对于均匀平面阵，激励可分离：

$$W_{2d}(m,n) = W_x(m) \times W_y(n)$$

x 方向等效 1D 角度映射：

$$\theta_x = \arccos(\sin\theta_0 \cos\varphi_0), \quad \theta_y = \arccos(\sin\theta_0 \sin\varphi_0)$$

Taylor 35dB 设计的 1D 副瓣全部 ≤ -41 dB，但在 2D 对角方向，两个 1D 过渡区值相乘（约 -15 dB × -15 dB = -30 dB），这是可分离 Taylor 的固有限制。

### 3.3 LCMV 自适应置零

LCMV 以参考权值为中心做最小修正：

$$\min_w \|w - w_{\text{ref}}\|^2 \quad \text{s.t.} \quad C^H w = f$$

关键修正：先归一化 $w_{\text{ref}}$ 使 $a_{\text{main}}^H w_{\text{ref}} = 1$，避免主瓣约束尺度不匹配。修正后 $\Delta\|w\| = 0.003$（几乎不改变 Taylor 权值），SLL 保持 Taylor 水平，4 个零陷 ≤ -38 dB。

### 3.4 SOCP 教师

$$\min_{w,t} \quad t \quad \text{s.t.} \quad a_{\text{main}}^H w = 1, \quad |a_i^H w| \leq t, \quad |a_{\text{null}}^H w| \leq \varepsilon$$

采用 CLARABEL 求解器 + 切平面迭代（粗网格求解 → 密集网格验证 → 加入最差副瓣点 → 重新求解），best-so-far 保护确保不恶化。

### 3.5 统一评估器

在均匀 uv 网格（101×101）上计算方向图，用 scipy.ndimage.label 做 -30dB 8 连通域主瓣检测，自动适应非圆形主瓣。同时计算 3×3dB_BW 对比口径。报告球面指向误差、目标点零深和 ±3° 零陷宽度。

---

## 四、典型结果

### 4.1 理想条件

#### 4.1.1 73 方向正式验收

| 指标 | 均值 | 最差 | 目标 | 通过率 |
|---|---|---|---|---|
| 和波束 SLL (3×3dB_BW) | -35.6 | -35.2 | ≤ -35 | **100% (73/73)** |
| LCMV 后 SLL | -35.6 | -35.1 | ≤ -35 | **100% (73/73)** |
| 零陷深度 (±3°) | -45.5 | -37.7 | ≤ -30 | **100% (73/73)** |
| 指向误差 | 0.24° | 0.46° | ≤ 0.15° | — |
| Δ‖w‖ (LCMV 改变量) | 0.003 | 0.010 | — | — |

#### 4.1.2 200 随机方向验证

| 指标 | 均值 | P95 | 最差 | 目标 | 通过率 |
|---|---|---|---|---|---|
| Taylor SLL | -35.3 | -35.2 | -35.1 | ≤ -35 | **100% (200/200)** |
| 指向误差 RMS | 0.028° | — | — | ≤ 0.195° | **100%** |

### 4.2 非理想条件

固定激励（按中心频率和理想阵列设计后不变），3 方向 × 20 种子：

| 条件 | SLL 均值 | SLL 最差 | 退化 | 通过率 |
|---|---|---|---|---|
| 理想 | -35.5 | -35.2 | 0 | 100% |
| 0.5dB + 6bit 量化 | -34.5 | -32.1 | +3.1 | 67% |
| ±λ/20 位置扰动 | -33.4 | -30.4 | +4.7 | 7% |
| 5% 阵元失效 | -31.5 | -29.4 | +5.8 | 0% |
| 10% 阵元失效 | -28.7 | -26.0 | +9.1 | 0% |
| 20% 阵元失效 | -25.5 | -23.1 | +12.1 | 0% |
| -10% 频偏 | -20.3 | -0.9 | +34.3 | 33% |
| +10% 频偏 | -24.7 | -2.5 | +32.7 | 67% |

### 4.3 AI 方向探索

#### 4.3.1 阵元失效补偿（负结果）

在 32×32 均匀平面阵、5% 阵元失效下，系统验证了 5 种方法：

| 方法 | SLL 均值 | SLL 最差 | 结论 |
|---|---|---|---|
| 无补偿 (Taylor+LCMV) | -31.5 | -29.4 | 基线 |
| 梯度下降 (sum-of-squares) | -25.8 | — | 降均值升峰值 ✗ |
| 梯度下降 (soft-max) | -9.0 | — | 破坏方向图 ✗ |
| WLS/IRLS (best-so-far) | -31.5 | -29.4 | 无改善 Δ=0 |
| SOCP 切平面 (best-so-far) | -31.8 | -30.8 | 无改善 Δ=+0.0 |

**结论**：5% 失效退化 4 dB 来自物理孔径缺口，非权值次优。SOCP（全局优化器）无法改善 Taylor，表明 Taylor 在均匀平面阵 + 失效条件下已接近全局最优。此负结果排除了"为 AI 而 AI"的失效补偿方向。

#### 4.3.2 非均匀坐标综合（负结果）

在 32×32 均匀阵列上加入独立 2D 位置扰动：

| 位置扰动 | 坐标 Taylor | SOCP | 改善 |
|---|---|---|---|
| ±0.05λ (≈ ±λ/20 竞赛标准) | -32.6 | -32.6 | +0.0 |
| ±0.10λ | -26.8 | -26.9 | -0.1 |
| ±0.20λ | -22.3 | -23.8 | -1.5 |

**结论**：在竞赛标准位置误差（±λ/20）下，坐标 Taylor 已最优，SOCP 无改善。仅在大扰动（±0.20λ，超出竞赛范围）下 SOCP 有 1.5 dB 改善。

#### 4.3.3 曲面阵列综合（正结果）

在 32×32 抛物面阵列（z = α(x²+y²)）上验证：

| 曲率 α | 坐标 Taylor | SOCP | 改善 | Taylor 退化 |
|---|---|---|---|---|
| 0.00 (平面) | -35.6 | -35.6 | +0.0 | 0 dB |
| 0.02 (微曲) | -35.6 | -35.6 | +0.0 | 0 dB |
| 0.05 (轻曲) | -26.3 | -26.3 | +0.0 | 9.3 dB |
| **0.10 (中曲)** | **-20.8** | **-24.9** | **-4.2** | 14.8 dB |
| **0.15 (大曲)** | **-17.1** | **-21.7** | **-4.6** | 18.5 dB |

**结论**：
1. 曲面阵列 Taylor 退化严重（α=0.15 时退化 18.5 dB），无可分离闭式解
2. SOCP 在 α ≥ 0.10 时稳定改善 4.2-4.6 dB
3. 曲面阵列是 AI 有明确增量价值的方向
4. SOCP ~23s/阵列 → AI 推理 0.504ms（纯）/0.658ms（端到端）→ 45,635 倍（纯推理）/34,954 倍（端到端）加速

#### 4.3.4 DeepSets AI 综合结果（正结果，已完成）

排列等变 DeepSets 网络（逐阵元编码→mean+max 池化→全局回传→ΔW 输出），280 个 SOCP 教师标签训练。

**v1 单扫描方向（θ=30°，128 维网络，116K 参数）：**

| 数据集 | Taylor | SOCP | AI | 恢复率 |
|---|---|---|---|---|
| 验证集(30) | -19.70 dB | -23.11 dB | -23.51 dB | 111.5% |
| 测试集(50) | -20.18 dB | -23.27 dB | -23.72 dB | 114.6% |

AI 超越 SOCP 教师（恢复率 >100%），学到 200 样本统计规律比单个 SOCP 更平滑。

**v2 多扫描方向（θ=0/15/30/45/60°，256 维网络，463K 参数）：**

| 数据集 | Taylor | SOCP | AI | 恢复率 |
|---|---|---|---|---|
| 验证集(30) | -21.65 dB | -24.14 dB | -23.87 dB | 89.3% |
| 测试集(50) | -21.92 dB | -23.98 dB | -23.62 dB | 82.4% |

各方向恢复率：0°/15° 正确预测无需修正，30°→86%，45°→97%，60°→60%。满足 80% 退出条件。

**排列等变性验证**：打乱阵元顺序→输出对应打乱→方向图不变，max_diff = 2.24×10⁻⁸（float32 精度极限）。

#### 4.3.5 圆柱面阵列验证

圆柱面 z = R·(1-cos(x/R))，y 方向平直，曲率仅在 x 方向：

| 半径 R | 等效 α | Taylor | SOCP | 改善 |
|---|---|---|---|---|
| 5 | 0.100 | -13.2 dB | -21.6 dB | -8.4 dB |
| 8 | 0.062 | -15.0 dB | -21.2 dB | -6.1 dB |
| 10 | 0.050 | -19.0 dB | -21.7 dB | -2.7 dB |
| 15 | 0.033 | -32.0 dB | -32.0 dB | 0.0 dB |
| 20 | 0.025 | -34.9 dB | -34.9 dB | 0.0 dB |

圆柱面 SOCP 改善比抛物面更大（R=5 时 -8.4 dB vs 抛物面 α=0.10 的 -4.2 dB），是 AI 另一有效方向。

#### 4.3.6 曲面+量化/失效联合实验

在曲面阵列上测试 Taylor vs SOCP vs AI 在非理想条件下的鲁棒性：

| 条件 | Taylor | SOCP | AI | AI 优势 |
|---|---|---|---|---|
| 理想 | -20.2 dB | -22.8 dB | -23.6 dB | -3.4 dB |
| 5% 失效 | -19.8 dB | -22.2 dB | -23.1 dB | -3.2 dB |
| 10% 失效 | -19.7 dB | -21.9 dB | -22.2 dB | -2.4 dB |
| 20% 失效 | -19.0 dB | -21.2 dB | -21.4 dB | -2.4 dB |
| 量化+5%失效 | -20.1 dB | -22.1 dB | -23.1 dB | -3.0 dB |

AI 在所有非理想条件下保持 2.3-3.4 dB 优势，0.5dB+6bit 量化对曲面阵列影响可忽略。

### 4.4 60° 扫描退化诊断

发现 60° 扫描 SLL 从 -35 dB 虚假退化到 -24 dB，系统诊断确认：

1. 1D Taylor 35dB 副瓣全部 ≤ -41 dB（设计值 35 dB）
2. 2×3dB_BW 排除在大扫描角下太小（主瓣裙边被误判为副瓣）
3. 改用 3×3dB_BW 后 SLL = -35.2 dB，全部达标
4. 对角方向过渡区乘积（-15 × -15 = -30 dB）是可分离 Taylor 固有限制
5. Taylor 参数扫描（35→45 dB）无法解决（过渡区不随副瓣变化）

### 4.5 NPU 部署与标准基准

- 硬件：Ascend 910_9362（单卡 64 GB HBM，20 Cube 核，40 Vector 核），鲲鹏 CPU（40 核 3.0 GHz），同一物理机 PCIe 直连
- 软件：PyTorch 2.7.1 + torch_npu 2.7.1，自动设备选择 NPU > GPU > CPU
- 61 项单元测试全部通过

**精度一致性**：CPU 与 NPU 输出最大绝对误差 7.45×10⁻⁸，余弦相似度 0.99999999，迁移到 NPU 不改变任何计算精度。

**训练加速（1024 阵元，30 轮计时，含 forward+backward+step）：**

| 模型维度 | batch | CPU(ms) | NPU(ms) | 加速比 |
|---|---|---|---|---|
| 128 | 16 | 38.03 | 2.42 | 15.7x |
| 256 | 32 | 214.97 | 3.13 | 68.6x |
| 512 | 64 | 1300.45 | 15.10 | 86.1x |

模型越大、batch 越大，NPU 加速比越高（3.7-86.1 倍）。NPU P95≈P50（极度稳定），CPU P95 可达 P50 的 2-3 倍（抖动大）。

**推理延迟（128 维，1024 阵元，batch=1，1000 轮）：**

| 指标 | NPU | CPU | 加速比 |
|---|---|---|---|
| mean | 0.504 ms | 2.726 ms | 5.4x |
| P50 | 0.502 ms | 1.332 ms | — |
| P95 | 0.521 ms | 8.590 ms | — |
| P99 | 0.532 ms | 13.272 ms | 25.0x |

NPU P99(0.53ms)≈P50(0.50ms)，极度稳定；CPU P99(13.27ms)是 P50(1.33ms)的 10 倍。

**端到端延迟**（CPU 生成输入→H2D→NPU 推理→D2H→CPU 拿结果，200 轮）：mean=0.658ms，P99=0.695ms。推理占 81.8%，数据传输占 18.2%。

**vs SOCP**：纯推理 0.504ms vs SOCP ~23s = 45,635 倍；端到端 0.658ms vs SOCP ~23s = 34,954 倍。（注：此为 AI 推理与 SOCP 求解的算法+硬件综合差距，非纯 NPU 硬件加速。）

**吞吐量**：256 阵元 batch=64 时 NPU 达 130,645 样本/秒；1024 阵元 batch=1 连续推理 NPU 1,985 样本/秒 vs CPU 367 样本/秒（9.0 倍）。

---

## 五、创新点

1. **修正原代码 7 项关键 Bug**：相位公式/单位/符号/accuracy 维度/loss mask/LCMV 尺度/索引类型，每项都有明确的"修正前→修正后"数值对比
2. **60° 扫描退化诊断**：排除方法问题而非 Taylor 问题，3×3dB_BW 口径下 100% 达标
3. **LCMV 主瓣约束尺度修正**：归一化 w_ref 使 SLL 从 -11 dB 恢复到 -35 dB
4. **方向自适应第一零点评估方法**：uv 均匀网格 + -30dB 8 连通域主瓣检测
5. **SOCP 系统验证**：均匀平面阵 + 失效/扰动下 Taylor 已接近最优（负结果），曲面阵列 SOCP 改善 4.2-8.4 dB（正结果），明确了 AI 的增量价值边界
6. **DeepSets 排列等变网络**：首次用于曲面阵列综合，排列等变性验证通过（max_diff=2.24×10⁻⁸），v1 恢复率 114.6%（AI 超越 SOCP 教师），v2 多方向 82.4%
7. **昇腾 910 NPU 全流程实现**：训练加速 3.7-86.1 倍，推理 0.504ms（P99=0.532ms），vs SOCP 45,635 倍加速，61 项测试全兼容

---

## 六、结论与展望

### 6.1 已完成

- 32×32（1024 阵元）Taylor 可分离基线：3×3dB_BW 口径下 SLL ≤ -35 dBc，73 方向 + 200 随机方向 100% 达标
- LCMV 自适应置零：4 个零陷 ≤ -38 dB，Δ‖w‖ = 0.003
- 非理想实验：量化退化 3 dB（可接受），失效退化 6-12 dB（物理限制），频偏退化 34 dB（需真时延）
- SOCP 负结果研究：均匀平面阵下 Taylor 已接近全局最优
- 曲面阵列正结果：SOCP 改善 4.2-4.6 dB，确认 AI 方向可行
- DeepSets AI 综合：v1 单方向恢复率 114.6%（超越 SOCP 教师），v2 多方向 82.4%，排列等变性验证通过
- 圆柱面阵列验证：SOCP 改善 -8.4 dB（比抛物面更大）
- 曲面非理想实验：AI 在所有非理想条件下保持 2.3-3.4 dB 优势
- NPU 全流程实现：训练 3.7-86.1 倍加速，推理 0.504ms（vs SOCP 45,635 倍），精度完全一致，61 项测试通过

### 6.2 展望

1. 圆柱面阵列 DeepSets AI 训练（改善更大，AI 潜力更大）
2. 阵元方向图和法向影响建模
3. 曲面阵列 84 方向正式验收
4. 曲面 + 失效联合 AI 补偿

---

## 参考文献

[1] Dolph C L. A current distribution for broadside arrays which optimizes the relationship between beam width and side-lobe level. Proc. IRE, 1946.
[2] Balanis C A. Antenna theory: analysis and design. John Wiley & Sons, 2016.
[3] Woodward P M, Lawson J D. The theoretical precision with which an arbitrary radiation-pattern may be obtained from a source of finite size. J. IEE Part III, 1948.
[4] Holland J H. Adaptation in natural and artificial systems. Univ. Michigan Press, 1975.
[5] Kennedy J, Eberhart R. Particle swarm optimization. Proc. ICNN, 1995.
[6] Storn R, Price K. Differential evolution – a simple and efficient heuristic for global optimization over continuous spaces. J. Global Optim., 1997.
[7] Bucci O M, Franceschetti G. On the spatial bandwidth of scattered fields. IEEE Trans. AP, 1989.
[8] Cui C, et al. An effective artificial neural network-based method for linear array beampattern synthesis. IEEE TAP, 2021.
[9] Zhou Z, et al. Two-order deep learning for generalized synthesis of radiation patterns for antenna arrays. IEEE TAI, 2022.
[10] Yang X, et al. Synthesis of Linear Sparse Array Using DNN-Based Machine-Learning Method. IEEE TAP, 2023.
[11] Raissi M, Perdikaris P, Karniadakis G E. Physics-informed neural networks. J. Comput. Phys., 2019.
[12] Liu R. 基于LSTM结构的变长度天线阵列综合方法. 2024.
[13] Hochreiter S, Schmidhuber J. Long short-term memory. Neural Computation, 1997.
[14] Cho K, et al. Learning phrase representations using RNN encoder-decoder. arXiv:1406.1078, 2014.
[15] Kingma D P. Adam: A method for stochastic optimization. arXiv:1412.6980, 2014.
