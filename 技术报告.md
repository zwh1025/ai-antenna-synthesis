# 基于人工智能的阵列天线综合技术研究报告

> 题目编号：XA-202604
> 发榜单位：中国电子科技集团公司第二十九研究所

## 摘要

本研究面向二维大规模阵列天线的方向图综合，采用物理硬指标、AI 快速综合和工程非理想评估相互补充的三层证据结构。物理轨道在 32×32、1024 阵元 planar array 上，以 Taylor/Bayliss 激励和 LCMV 置零完成 Sum/Difference 与理想 adaptive-null 验收；最新 upstream v3 主线在 1024 阵元平面/曲面任务上提供了平面增广、真实 Ascend 910 基准和 4096 阵元计算规模测试。另有一组固定 reconstructed curved geometry 的独立-parent 研究，量化了 AI 的 held-out improvement、sample efficiency、收敛轨迹和 CPU 在线耗时。位置误差、幅相量化、阵元失效、频率偏移及 known-mask 权值重构作为独立 robustness evidence 报告。不同层次的 geometry、模型、评估器和数据划分不完全相同，本文不把它们包装为一个单一 benchmark。

**关键词**：阵列天线综合；Taylor；Bayliss；LCMV；SOCP/cutting-plane；DeepSets；鲁棒性；样本效率

## 一、研究背景与问题定义

阵列天线综合需要同时处理阵元规模、扫描范围、主瓣保持、副瓣控制、零陷约束和计算成本。Taylor、Chebyshev、Bayliss 等解析方法在规则平面阵上计算高效；LCMV 可在保持主瓣响应的同时加入零陷约束；SOCP/cutting-plane reference 可处理更一般的复权值与场约束，但逐场景求解成本较高。深度学习则有机会把离线优化得到的 correction 学习为一次前向计算。

本研究围绕以下冻结要求展开：二维平面或曲面阵列、阵元数不少于 1000、输入阵元坐标、上半空间 ±60° 扫描，以及 Sum/Difference、adaptive null、综合效率和位置/量化/失效/频率非理想评估。最终硬指标采用 official evaluator 1.0.0；upstream v3 的 legacy uv-domain evaluator 作为其自身历史与 v3 验收路径使用，二者不在没有说明的情况下混用。

## 二、三层证据结构

### 2.1 Track P：物理硬指标综合

Track P 使用 32×32、1024 阵元 planar geometry，Taylor 负责 Sum taper，Bayliss 负责 Difference excitation，LCMV 负责 adaptive null。该轨道承担理想硬指标以及后续 robustness/failure reconstruction 的物理基线。

### 2.2 Track AI-v3：最新 upstream AI/NPU 主线

upstream v3 使用平面样本增广改善模型的平面分布外表现，并在 1024 阵元平面/曲面任务上进行验收；Ascend 910 的真实权重基准属于该 v3 模型。4096 阵元结果是计算规模测试，不是全波电磁验证。

### 2.3 Track AI-fixed：固定曲面任务的独立研究

该补充研究只针对一个固定的 32×32、1024 阵元 reconstructed curved geometry：

```text
coordinate-aware Taylor baseline
        → SOCP/cutting-plane optimization reference
        → DeepSets residual learning
        → calibrated residual correction
```

它用于研究 independent teacher-parent 数量、held-out target improvement 和 convergence，不替代也不直接 benchmark upstream v3 的 mixed-geometry/NPU workflow。

## 三、物理方法与评估器

### 3.1 阵因子与解析激励

方向图由实际阵元坐标计算。平面阵采用可分离 Taylor Sum excitation 和 Bayliss Difference excitation；曲面任务使用 coordinate-aware Taylor 作为基线。对三维坐标，阵因子可写为：

$$
F(\theta,\varphi)=\sum_n w_n\exp\{j k(x_nu+y_nv+z_nw)\},
$$

其中 $u=\sin\theta\cos\varphi$、$v=\sin\theta\sin\varphi$、$w=\cos\theta$。相位和单位在各冻结 evaluator 中保持一致。

### 3.2 LCMV adaptive null

LCMV 以参考权值为中心求解：

$$
\min_w\|w-w_{\mathrm{ref}}\|_2^2
\quad\mathrm{s.t.}\quad C^H w=f .
$$

理想模型加入主瓣响应和至少 4 个主瓣外 null constraints。非理想与失效实验使用独立冻结的退化/重构定义，不能把理想 strict-null 结果外推到所有扰动条件。

### 3.3 评估口径

Track P 的最终硬指标使用 official evaluator 1.0.0。理想 Difference intrinsic null 达到模型数值精度下限，正式展示为低于 `-100 dBc` 的 numerical floor，不把数值下限当作硬件真实性能。upstream v3 的验收表仍保留其 3×3dB_BW legacy uv-domain 口径，并单独披露口径差异。

## 四、Track P 理想条件结果

| 指标 | 冻结结果 | 要求 | 状态 |
|---|---:|---:|---|
| Sum SLL（Taylor） | worst `-35.156576 dBc`；273/273 | ≤ -35 dBc | PASS |
| Sum SLL（LCMV，regular 73） | worst `-35.018982 dBc`；73/73 | ≤ -35 dBc | PASS |
| Azimuth Difference SLL | worst `-21.754833 dBc`；273/273 | ≤ -20 dBc | PASS |
| Elevation Difference SLL | worst `-21.638634 dBc`；273/273 | ≤ -20 dBc | PASS |
| Difference intrinsic null | ideal numerical floor；273/273 | ≤ -30 dBc | PASS |
| Difference pointing | 两轴均 273/273 | ≤ BW_sum_3dB/30 | PASS |
| Ideal strict adaptive null（Sum） | 73/73；全部 `-300 dBc` | ≤ -65 dBc | PASS |
| Difference adaptive null | 73/73；全部 `-300 dBc`；置零后 SLL worst `-21.755 dBc` | 4×≤-30 与 SLL≤-20 联合 | PASS |

全量 273 方向基线与差波束自适应零陷闭环的逐案例数据位于 `results/stage2_strict_closure/` 与 `results/stage2b_diff_adaptive_closure/`。差波束零陷由 `capon_nulling_difference_2d`（含内在零点保护约束）实现，关闭了原 baseline 的 `BASELINE_NOT_IMPLEMENTED` 项。以上是 Track P physics evidence，不是 DeepSets direct-output 的同任务结果。

## 五、Track AI-v3 结果

### 5.1 平面/曲面覆盖

upstream v3 的 `planar_fix_v3.json` 记录：平面增广后服务器复现的 40 个方向最大退化为 `+0.34 dB`，曲面测试集恢复率为 `120.2%`。这说明该 v3 训练流程改善了其固定模型的平面分布外表现，但不等同于跨 geometry 泛化。

### 5.2 平面验收

v3 平面验收有两套口径，均按测试臂单独报告：

**legacy 3×3dB_BW 口径**（`acceptance_v3_ai.json`）：

| 测试臂 | 标准方向 SLL | 随机方向 SLL | 说明 |
|---|---:|---:|---|
| Taylor+LCMV baseline | 73/73 | 195/200 | 解析基线 |
| v3 direct | 70/73 | 199/200 | model output only |
| v3+LCMV | 69/73 | 194/200 | model output plus LCMV |

**official 第一零点口径**（`results/stage2_ai_v3_official/`，与 Track P 基线同 273 方向、同评估器）：

| 测试臂 | 标准方向 SLL | 随机方向 SLL | 自适应零陷 |
|---|---:|---:|---|
| v3 direct | 69/73（worst `-34.726`） | 196/200 | — |
| v3+LCMV | 61/73（worst `-34.626`） | 186/200 | 73/73 全过（`-300 dBc`） |

第一零点口径较 legacy 口径更严：LCMV 置零在该口径下引入 0.05–1.1 dB 代价，且多方向基线裕量本就 <0.1 dB。两套口径下 v3 均未 100% 达标，这些数据应按测试臂理解，不能简化为 v3 direct output 在所有硬指标上 100% 通过；未达标方向由安全门控（AI 不劣于 Taylor 则导出，否则回退解析基线）兜底。4096 阵元仅用于 DeepSets 计算规模测试。

### 5.3 NPU benchmark

upstream v3 的真实权重在 Ascend 910 上报告：纯推理 mean `0.499 ms`、P99 `0.532 ms`，端到端 mean `0.632 ms`、P99 `0.664 ms`；1024 阵元 batch=64 吞吐约 `27,649 samples/s`，CPU/NPU 余弦相似度为 `1.0`。这是 v3 256-dim 模型、指定硬件和指定计时边界下的实测，不是所有模型或所有 SOCP 工作流的普适加速比。

### 5.3.1 AI效率四项指标（竞赛方案明文要求）

**2a 收敛速度**（`convergence_v3.json`）：v3 训练第 100 epoch（265 s）曲面 SLL 进入最终最优值 0.5 dB 内；总训练 751 s（250 epochs 早停）；Stage5A 固定任务 90% 增益需 55–70 epochs。

**2b 单次合成时间与加速比**（12算例独立验收口径，`teacher_stage0a.json`）：Taylor −14.2~−35.1 dB/<1 ms；**AP −0.5~−19.3 dB/10.8 s（AI 加速 21,625×，且独立口径下 AI 副瓣优于 AP——AP 的 −34.97 dB 设计网格数字经独立验收证实为采样伪影）**；GA −22.60 dB/35 s（70,140×）；PSO −24.70 dB/81.9 s（164,128×）；SOCP（弱教师）−19.5~−22.8 dB/13 s（46,080×）；密集SOCP＋范数约束（离线教师验真）−21.9~−37.2 dB/14~23 min；AI v3 −23.3 dB/0.499 ms（NPU）。AP 为竞赛方案明文列出的迭代算法。

**2c 模型复杂度**（`model_complexity_v3.json`）：参数 463,106；MACs 339,148,800；FLOPs 678,297,600；模型 1.86 MB；NPU 0.499 ms/CPU 2.762 ms——资源受限平台可实时部署。

**2d 样本效率**：Stage 5A（4 个独立 teacher parents 起有效改善，6–8 个约 +1.3 dB），见第六章。

## 六、Track AI-fixed：样本效率与收敛

固定曲面任务的 Stage 5A 采用 2/4/6/8 independent teacher parents、2 个 validation parents、4 个 test parents 和 3 个 fixed seeds。每个 parent 的 D4 rows 是 correlated augmentation，不增加 independent teacher count；model/seed selection 只使用 validation。

| Independent parents | D4 rows | Test improvement | Mean gain vs Taylor |
|---:|---:|---:|---:|
| 2 | 16 | 3/4 nominal | approximately 0 dB |
| 4 | 32 | 4/4 | approximately 0.837 dB |
| 6 | 48 | 3/4 | approximately 1.351 dB |
| 8 | 64 | 4/4 | approximately 1.326 dB |

因此公开结论为：从 4 个 independent teacher parents 开始出现具有实际意义的 held-out improvement，6–8 个 parents 的平均改善约 1.3 dB。4 个独立 test parents 的正式固定曲面结果为 4/4 改善，平均增益 `1.1876 dB`。严格的全 validation-row `0.5 dB` teacher-proximity 条件未达到；best epoch、early-stop、90%-gain epoch 和 training runtime 已记录。

固定曲面模型的 complexity 为 `597,250 parameters`、`405,733,376 MACs`、`811,466,752 FLOPs`，仅指 neural forward。复用的 CPU online latency 为 inference-only mean `9.157 ms`、end-to-end mean `9.756 ms`。SOCP/cutting-plane reference generation 约 `13.854 s/independent parent` 属于 offline cost，与 online latency 不混合；经验比约 `1.42×10^3` 只适用于该固定任务的两种计时边界。

## 七、Robustness 与 failure reconstruction

（全部数字来自本地冻结工件 `results/stage4a_robustness_degradation/` 与 `results/stage4b_failure_reconstruction/`，官方评估器 1.0.0。）

### 7.1 Degradation

固定 frozen cases 的补充结果为：

| 条件 | 冻结结果 |
|---|---:|
| Position error `±0.05λ` | mean Sum-SLL `-32.986 dBc`（degradation `+2.171 dB`，worst `-29.818`） |
| 0.5 dB amplitude + 6-bit phase | mean `-34.312 dBc`（degradation `+0.845 dB`） |
| 5%/10%/20% failures | 51/102/204 failed elements |
| Failure mean degradation | `+4.197/+6.390/+9.952 dB` |
| Frequency ratio sweep | `0.90–1.10`；common-joint `76/336`；full band `3/16`；16/16 案例存在含 $f_0$ 的合规带；平均合规带宽 22.6% |

这些是 degradation evidence，不表示非理想条件下仍全部满足理想硬指标。upstream v3 另有 3 directions × 20 seeds 的 legacy fixed-excitation 表，两套采样和 evaluator 不合并。

### 7.2 Known-mask reconstruction（竞赛"失效下权值修正能力"）

B2 active-set reconstruction 对 5%–20% failure 的平均 Sum-SLL recovery 为 `2.684/3.011/2.094 dB`（如 5%：−30.96→−33.64 dBc）。全部 960 个 mask 的 common-joint hard-spec pass 仅 `2/960`，因此结论是 partial/limited recovery，而非 full restoration。Azimuth Difference 已运行；Elevation Difference failure reconstruction 未正式运行。完整恢复需非理想条件化重综合（后续工作）。

## 八、贡献、边界与局限

本研究的可审查贡献包括：

1. 1024 阵元 Track P 的 Sum/Difference、ideal adaptive null（含差波束自适应零陷闭环）和 official evaluator 闭合；
2. upstream v3 的 1024 AI/NPU/4096-scale evidence，含 legacy 与 official 双口径的平面验收；
3. 固定 1024 curved task 上 independent-parent sample-efficiency 与 convergence evidence；
4. 位置、幅相、failure、frequency degradation 及 known-mask partial recovery。

以下结论不应从当前证据推出：

- fixed curved study 不证明跨几何体或未见任务的 generalization；
- Track P 的 hard-spec closure 不应归因于 DeepSets direct output；
- `0.499/0.632 ms` 是 upstream v3 Ascend 910 benchmark，`9.157/9.756 ms` 是另一固定曲面 CPU 模型，不能直接排名；
- SOCP/cutting-plane 是 reference，不作为普适最优性证明；
- failure reconstruction 的 full hard-spec recovery 仍有限；
- 8×8 HFSS 结果用于端口映射和 EEP 重组工程边界，不能外推为 64×64 全波性能。

## 参考文献

[1] Dolph C L. A current distribution for broadside arrays which optimizes the relationship between beam width and side-lobe level. Proc. IRE, 1946.
[2] Balanis C A. Antenna Theory: Analysis and Design. John Wiley & Sons, 2016.
[3] Woodward P M, Lawson J D. The theoretical precision with which an arbitrary radiation-pattern may be obtained from a source of finite size. J. IEE Part III, 1948.
[4] Kennedy J, Eberhart R. Particle swarm optimization. Proc. ICNN, 1995.
[5] Storn R, Price K. Differential evolution. J. Global Optim., 1997.
[6] Bucci O M, Franceschetti G. On the spatial bandwidth of scattered fields. IEEE Trans. AP, 1989.
[7] Cui C, et al. An effective artificial neural network-based method for linear array beampattern synthesis. IEEE TAP, 2021.
[8] Zhou Z, et al. Two-order deep learning for generalized synthesis of radiation patterns for antenna arrays. IEEE TAI, 2022.
[9] Yang X, et al. Synthesis of Linear Sparse Array Using DNN-Based Machine-Learning Method. IEEE TAP, 2023.
[10] Raissi M, Perdikaris P, Karniadakis G E. Physics-informed neural networks. J. Comput. Phys., 2019.
[11] Liu R. 基于 LSTM 结构的变长度天线阵列综合方法. 2024.
[12] Hochreiter S, Schmidhuber J. Long short-term memory. Neural Computation, 1997.
[13] Cho K, et al. Learning phrase representations using RNN encoder-decoder. arXiv:1406.1078, 2014.
[14] Kingma D P, Ba J. Adam: A method for stochastic optimization. arXiv:1412.6980, 2014.
