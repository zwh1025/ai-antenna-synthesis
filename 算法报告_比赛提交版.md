# 基于人工智能的阵列天线综合算法报告（比赛提交版）

> 题目编号：XA-202604  
> 版本基线：Git commit `2884fae`（2026-09-04）  
> 评估口径：正式结果采用 official evaluator 1.0.0（第一零点严格口径）；历史3×3dB_BW口径数字保留作对照并明确标注

---

## 1. 算法目标

算法接收阵元三维坐标、扫描方向、期望副瓣、零陷方向及可选非理想状态，输出每个端口的复激励权值。设计目标包括：

1. 支持不少于1000阵元的二维平面阵或曲面阵列；
2. 覆盖$\theta\le60^\circ$的圆锥扫描（上半空间）；
3. 在规则阵列上满足和差波束与多零陷硬约束（和波束零深−65 dBc、差波束零深−50 dBc严格门槛）；
4. 在任意坐标阵列上用AI逼近迭代优化器；
5. 在线推理达到毫秒或亚毫秒级；
6. AI结果必须经过物理复算和安全门控后才能导出。

**目标达成情况**：性能硬指标在官方口径下全部通过（273/273，详见技术报告§1.2）；效率四项指标（收敛速度、加速比含交替投影法、模型复杂度、样本效率）全部实测完成（§10.2）；鲁棒性完成量化退化审计与失效重构。

最终系统采用混合策略：规则平面阵使用Taylor＋LCMV可靠基线；曲面和任意坐标阵列使用"坐标基线＋DeepSets残差"；不安全结果混合Taylor或拒绝导出。v3模型经平面样本增广后，单一网络在平面与曲面阵列上均达到可用精度（平面最差退化+0.51 dB，曲面恢复率120%并超过教师）。

---

## 2. 数据定义与相位约定

### 2.1 输入

第$n$个阵元的局部输入包括：

$$
\mathbf x_n=[x_n,y_n,z_n,\Re(w_{0,n}),\Im(w_{0,n}),u_0,v_0,w_0,SLL_{target}],
$$

其中$w_{0,n}$是解析或坐标Taylor基线，$(u_0,v_0,w_0)$是目标扫描方向余弦。

### 2.2 输出

网络输出复权值残差：

$$
\Delta w_n=\Delta w_{n,re}+j\Delta w_{n,im},\qquad
w_n=w_{0,n}+\Delta w_n.
$$

输出统一做主瓣响应和总功率归一化。阵因子代码使用的模型权值与HFSS端口入射波之间满足：

$$a_{HFSS}=\operatorname{conj}(w_{model}).$$

该约定已经固化在8×8全波重组程序中。

---

## 3. 解析基线与物理层

### 3.1 三维阵因子

$$
F(\theta,\varphi)=\sum_{n=1}^{N}w_n
e^{jk(x_nu+y_nv+z_nw)},
$$

其中$u=\sin\theta\cos\varphi$、$v=\sin\theta\sin\varphi$、$w=\cos\theta$。

### 3.2 平面阵Taylor权值

32×32均匀平面阵的二维权值通过两个一维Taylor窗外积得到：

$$W_{2D}=W_x\otimes W_y.$$

扫描相位直接由阵元坐标和目标方向余弦产生。该方法计算快、可解释，是平面规则阵列的参考解和安全回退解。

### 3.3 LCMV置零

在尽量保留Taylor形状的条件下加入主瓣和零陷约束：

$$
\min_{\mathbf w}\|\mathbf w-\mathbf w_{ref}\|_2^2,
\quad\text{s.t.}\quad\mathbf C^H\mathbf w=\mathbf f.
$$

求解前先将$\mathbf w_{ref}$归一化到主瓣响应为1。该步骤修正了早期版本中主瓣约束尺度不一致导致的副瓣恶化。

---

## 4. SOCP教师生成

### 4.1 优化模型

SOCP最小化副瓣区域最大响应，并保留主瓣与零陷约束：

$$
\begin{aligned}
\min_{\mathbf w,t}\quad &t\\
\text{s.t.}\quad&a_0^H\mathbf w=1,\\
&|a_i^H\mathbf w|\le t,\quad i\in\Omega_{SL},\\
&|a_q^H\mathbf w|\le\epsilon_q,\quad q\in\Omega_{null}.
\end{aligned}
$$

### 4.2 切平面流程

1. 在15×15粗网格建立初始约束；
2. 使用CLARABEL求解复权值；
3. 在更密网格重新评价方向图；
4. 将最差副瓣点加入约束集合；
5. 最多迭代5轮，保存best-so-far权值；
6. 若优化结果不优于基线，则保留基线并记录失败原因。

SOCP单场景约23 s，适合作为离线教师，不适合作为在线实时综合器。

---

## 5. DeepSets网络

### 5.1 选择理由

阵元集合没有天然顺序。若交换两个阵元的输入位置，正确算法的输出权值也应交换，而总方向图保持不变。固定尺寸MLP难以处理不同阵元数，CNN又依赖规则栅格。DeepSets通过共享逐元素网络和集合池化满足该结构要求。

### 5.2 网络结构

设逐阵元编码为$h_n=\Phi(\mathbf x_n)$，全局特征为

$$
g=\rho\left([\operatorname{mean}_n(h_n),\operatorname{max}_n(h_n)]\right).
$$

逐阵元解码器计算

$$
[\Delta w_{n,re},\Delta w_{n,im}]
=\psi([h_n,g]).
$$

共享的$\Phi$和$\psi$使参数量不随阵元数量线性增长。v1采用128维隐藏层、约11.7万参数；v2采用256维隐藏层、约46.3万参数。

### 5.3 损失函数

当前训练以复权值残差监督为主：

$$
\mathcal L_w=\frac1N\sum_n
\left[(\Delta w_{n,re}-\Delta w^*_{n,re})^2+
(\Delta w_{n,im}-\Delta w^*_{n,im})^2\right].
$$

验证阶段不只比较MSE，而是重新计算主瓣、副瓣、零陷和指向误差。这样可以避免“权值误差小但方向图不合格”的情况。

---

## 6. 数据集与划分

### 6.1 原1024阵元曲面数据

| 版本 | 扫描设置 | 总样本 | Train/Val/Test | 用途 |
|---|---|---:|---:|---|
| v1 | $\theta=30^\circ$ | 280 | 200/30/50 | 单方向可行性 |
| v2 | $\theta=0/15/30/45/60^\circ$ | 280 | 200/30/50 | 多方向泛化 |
| v3 | v2＋120个平面样本（$\alpha=0$） | 400 | 320/30/50 | 修复捷径学习 |
| v4 | v3＋100个4096样本（60曲面SOCP教师$\alpha\in[0.02,0.15]$＋40平面）＋120个平面增广（$\theta$偏重45/60°） | 620 | 混合 | 规模泛化（1024+4096单一模型） |

每个样本包含阵元坐标、Taylor基线、SOCP权值、扫描参数及方向图指标。v1–v3为1024阵元；v4新增60+40个4096阵元样本（特征缩放采用$\times N$语义：训练常数1024恰为$N_{train}$，故$\times N$是"相对幅度"语义的自然推广，v4对1024样本与v3特征完全等价）。

**v3的由来（捷径学习修复）**：v2训练集中扫描角与修正需求完全相关（$\theta\le15^\circ$时SOCP零改善、$\theta\ge30^\circ$时改善3.5至4 dB），导致模型在理想平面阵上$\theta\ge30^\circ$分布外推理退化10.2至15.7 dB。加入平面样本打破该伪相关后，平面阵40方向最差退化降至+0.51 dB（本地）/ +0.34 dB（服务器复现），曲面测试集恢复率由82.4%提升至128%（本地）/120%（服务器复现）。详见`run_planar_generalization.py`、`run_planar_fix_train.py`和`planar_fix_v3.json`。

### 6.2 8×8专用数据与安全归档

8×8后续实验采用真实HFSS坐标生成专用SOCP教师，并使用按角度块划分、球面保护带和D4对称扩增，防止相邻方向泄漏。最终安全归档包含544个D4扩展方向：

| 模式 | 数量 | 可导出 |
|---|---:|---|
| `ai` | 328 | 是 |
| `ai_alpha` | 208 | 是 |
| `rejected_after_alpha` | 8 | 否 |

阵因子层安全率为536/544＝98.5%。这只表示通过阵因子安全门槛，不能提前视为HFSS全波通过。

---

## 7. D4对称包装与支持域门控

### 7.1 D4变换

方阵具有4种旋转和4种镜像/旋转组合。包装器将全方位目标映射到Canonical区域，调用一次网络后再按严格双射置换权值。

验证结果包括：

- 1440个全方位角度映射全部通过；
- 8个变换ID均出现；
- 35组D4不变量测试全部通过；
- 544个Block Test方向中最终536个通过阵因子门槛。

### 7.2 安全模式

推理结果分为：

1. `ai`：AI权值直接通过物理验收；
2. `ai_alpha`：按$w=\alpha w_{AI}+(1-\alpha)w_{Taylor}$混合后通过；
3. `taylor`：AI无安全解但Taylor安全；
4. `rejected_after_alpha`：遍历alpha仍不满足条件，禁止导出；
5. `degraded/unsupported`：方向位于已知退化或不支持区域。

实际部署时支持域标签必须由坐标与扫描方向查询或单独分类，不能直接使用测试集真值标签。

---

## 8. 训练配置与复现

### 8.1 环境

- Python 3.11
- PyTorch 2.7.1＋torch_npu 2.7.1
- Ascend 910_9362，单卡64 GB HBM
- NumPy 1.26、SciPy 1.17、CVXPY 1.9、CLARABEL

### 8.2 关键程序

| 文件 | 作用 |
|---|---|
| `project/mylib/deepsets.py` | DeepSets网络 |
| `project/run_generate_teacher.py` | 单方向SOCP教师生成 |
| `project/run_multi_scan_generate.py` | 多方向教师生成 |
| `project/run_deepsets_train.py` | 训练、早停和三方评价 |
| `project/run_planar_generalization.py` | 平面阵分布外泛化测试（捷径学习发现） |
| `project/run_planar_fix_train.py` | 平面增广v3训练与双场景验证 |
| `project/run_acceptance_v3_ai.py` | v3平面验收（legacy 3×3dB_BW口径） |
| `project/run_convergence_v3.py` | 收敛速度追踪（效率指标2a） |
| `project/run_model_complexity.py` | 模型复杂度统计（效率指标2c） |
| `project/run_ap_compare.py` | 交替投影法与六方法同口径对比（效率指标2b） |
| `project/run_benchmark.py` | CPU/NPU标准基准 |
| `project/run_benchmark_supplement.py` | 端到端和长轮次推理 |
| `project/run_benchmark_v3.py` | v3真实权重NPU基准（含一致性） |
| `project/run_ga_pso_compare.py` | Taylor/GA/PSO/SOCP/AI对比 |
| `project/run_stage2_strict_closure.py` | 物理基线273方向官方口径冻结 |
| `project/run_stage2_ai_v3_official.py` | v3三臂官方口径验收 |
| `project/run_stage2b_diff_adaptive_closure.py` | 差波束自适应零陷闭环 |
| `project/run_stage4a_robustness_degradation.py` | 鲁棒性退化审计（位置/量化/失效/频偏） |
| `project/run_stage4b_failure_reconstruction.py` | 失效感知权值重构（B0/B1/B2） |
| `project/run_stage5a_sample_efficiency.py` | 样本效率与收敛证据（效率指标2d） |
| `project/run_scale_4096.py` | 4096规模外推诊断（v3失效+语义缩放发现） |
| `project/run_scale_fix_v4.py` | v4混合规模训练与四象限验证 |
| `deepsets_8x8_d4_v2_1.tar.gz` | 8×8冻结模型、安全预测和支持域归档 |

### 8.3 复现顺序

```bash
cd project
python -m pytest tests/ -v
python run_acceptance_v2.py
python run_random_validation.py
python run_generate_teacher.py
python run_multi_scan_generate.py
python run_deepsets_train.py
python run_planar_generalization.py   # 复现v2平面退化(捷径学习)
python run_planar_fix_train.py        # v3增广修复训练与验证
python run_convergence_v3.py          # 收敛速度(效率2a)
python run_model_complexity.py        # 模型复杂度(效率2c)
python run_ap_compare.py              # AP六方法对比(效率2b)
python run_benchmark.py
python run_benchmark_supplement.py
python run_benchmark_v3.py            # 需NPU环境+deepsets_model_v3_256.pt
python run_stage2_strict_closure.py   # 物理基线官方口径冻结(273方向)
python run_stage2_ai_v3_official.py   # v3三臂官方口径验收
python run_stage2b_diff_adaptive_closure.py
python run_stage4a_robustness_degradation.py   # 鲁棒性退化审计
python run_stage4b_failure_reconstruction.py   # 失效重构
python run_scale_4096.py              # 4096外推诊断(~15min)
python run_scale_fix_v4.py            # v4混合规模训练(教师生成~57min+训练~18min)
```

训练数据和权值文件体积较大，由`.gitignore`排除；提交时应将模型、数据校验值及获取方式放入单独交付包，不能只提交生成脚本。

---

## 9. 评价方法

### 9.1 阵因子评价

评价分两套口径，正式结果一律采用前者：

**official evaluator 1.0.0（正式口径）**：第一零点UV包络（四方向剖面插值+显式fallback）、差波束双瓣包络、零陷中心连续响应（区别于3°窗口最坏值的诊断量）、大圆截面3dB波束宽度、球面指向误差、版本化结果schema（含哈希）。冻结定义见`docs/metric_evaluator_freeze.md`，实现为`project/mylib/official_evaluator.py`。

**legacy口径（对照/诊断）**：uv网格评估器输出主峰方向、球面指向误差、副瓣电平（−30dB连通域与3×3dB_BW双定义）、零陷深度、权值范数与安全状态。旧`random_validation.json`的first-null字段实为连通域别名，在大扫描角存在主瓣截断伪影，已弃用（`run_random200_taylor_3bw.py`与官方口径基线已补齐该缺口）。

评估口径属于测量定义，必须与数值同时记录；两套口径数字不混用。

### 9.2 全波评价

已求解EEP基函数记为$\mathbf E_n(\theta,\varphi)$（单端口激励、其余63端口接匹配负载——互耦、地板、曲面遮挡均含于其中），总场由线性叠加精确重组：

$$
\mathbf E_{tot}=\sum_{n=1}^{64}a_n\mathbf E_n.
$$

重组与HFSS直接仿真的等价性由一次确定性权值（Taylor）的直接求解交叉验证，误差$10^{-9}$ dB量级。多端口网络的反射波为$\mathbf b=\mathbf S\mathbf a$，总接受功率为

$$
P_{acc}=\mathbf a^H\mathbf a-\mathbf b^H\mathbf b.
$$

每个方法统一归一化到1 W入射功率。逐端口有源反射系数可能因互耦功率交换出现幅值大于1，最终判断需结合总接受功率，不能只看单个端口。

---

## 10. 算法结果

### 10.1 1024阵元AI质量

| 测试 | Taylor | SOCP | AI | 结论 |
|---|---:|---:|---:|---|
| v2验证集 | −21.65 | −24.14 | −23.87 | 恢复89.3% |
| v2测试集 | −21.92 | −23.98 | −23.62 | 恢复82.4% |
| v3测试集（平面增广后，同样本） | −21.92 | −23.98 | −24.56 | 恢复128.0% |
| 典型曲面算例 | −18.5 | −21.8 | −24.2 | AI较Taylor改善5.7 dB |

v3服务器独立重训版为−24.40 dB（恢复率120.2%），与本地版同量级，修复效果可复现。典型算例中AI超过当前弱SOCP结果，是因为SOCP仅使用有限粗网格和切平面轮数；这不表示神经网络超过同一连续问题的严格全局最优。

**平面阵分布外泛化（v3）**：理想32×32平面阵40个扫描方向（$\theta=0$–$60^\circ\times\varphi$ 8方位），v3最差退化+0.51 dB（本地）/+0.34 dB（服务器复现）；对照v2在$\theta\ge30^\circ$退化+10.2至+15.7 dB。

**规模泛化（v4，1024→4096，`scale_fix_v4.json`）**：v3直接外推4096曲面退化+2.2至+14.3 dB（修正量与训练孔径耦合、坐标/权值特征双外偏）；v4混合规模训练后单一模型四象限达标——4096平面+0.11 dB、4096曲面（$\alpha$=0.03/0.12，$\theta$=60°）反超Taylor 3.11/1.68 dB，且1024曲面恢复率提升至134%（v3为128%）、1024平面最差退化+0.46 dB（v3为+0.51）。4096 SOCP单场景49.6 s。

**v4服务器实测（`server_v4_benchmark.json`、`server_v4_acceptance.json`，2026-09-05）**：NPU纯推理@1024为0.502 ms（与v3的0.499 ms持平——混合规模训练零延迟代价）；**@4096为1.242 ms（805样本/s，相对4096 SOCP约40,000×）**；批量bs64 27,410样本/s；CPU/NPU一致性max_err=7.45×10⁻⁷、cos_sim=1.0。官方口径273方向验收：v4 direct 68/73+198/200（最差−34.35）、**v4+LCMV 65/73+190/200（最差−34.09，较v3的61/73+186/200双提升**——部署管线臂为最终交付口径）。

**平面阵竞赛验收实测（v3）**：在与解析系统相同的73标准方向＋200随机方向、相同评估器（3×3dB_BW口径）下，v3直接输出SLL通过70/73（−34.73最差）和199/200（−34.96最差）；v3＋LCMV为69/73和194/200（对照Taylor+LCMV 73/73和195/200）。未达标方向集中于$\theta=60^\circ$边缘（差≤0.36 dB，基线自身裕量仅0.09 dB）；零陷与指向与基线完全一致（精细指向RMS 0.028°）。结论：AI管线与解析管线性能等价，配合安全门控后混合系统满足全部硬指标。见`acceptance_v3_ai.json`。

### 10.2 AI效率四项指标（效率指标2a–2d完整数据）

**2a 收敛速度**（`convergence_v3.json`）：v3训练第100个epoch（265 s）曲面测试SLL进入最终最优值0.5 dB以内（−23.90 vs −24.28），总训练耗时751 s（250 epochs早停，本地CPU）；固定曲面任务Stage5A达到最终增益90%需55–70 epochs（107–153 s）；推理为单次前向，无迭代过程。

**2b 单次合成时间与加速比**（`benchmark_v3.json`、`ap_compare.json`）：

| 方法 | 曲面SLL（独立验收口径） | 单场景耗时 | AI(NPU)加速比 |
|---|---:|---:|---:|
| Taylor（解析） | −14.2 ~ −35.1 dB | <1 ms | 同量级（均亚毫秒） |
| 交替投影法（AP） | −0.5 ~ −19.3 dB | 10.8 s | 21,625× |
| GA | −22.60 dB（单算例，旧口径） | 35 s | 70,140× |
| PSO | −24.70 dB（单算例，旧口径） | 81.9 s | 164,128× |
| SOCP（弱教师） | −19.5 ~ −22.8 dB | 13 s | 46,080× |
| 密集SOCP＋范数约束（离线） | −21.9 ~ −37.2 dB | 14~23 min | — |
| **DeepSets v3/v4（NPU）** | **−23.3 ~ −24.7 dB** | **0.499 ms** | — |

（12个代表算例：$\alpha\in[0.05,0.15]$、$\theta\in[0,60]$，独立验收器=201×201＋错位＋随机＋峰值细化，`teacher_stage0a.json`）

**AP结果修正（阶段0A验真，2026-09-05）**：AP此前报告的−34.97 dB为设计网格自评的**采样伪影**——独立网格上AP退化至−0.5~−19.3 dB（网格间副瓣重现），权值范数失控（2–21×Taylor）、零陷邻域最差−3.8 dB；独立口径下AI副瓣与速度均优于AP。AP热启动SOCP实测无加速（凸问题性质）。带范数约束的密集切平面SOCP（41×41＋15轮＋零陷−35＋$\|\mathbf w\|_2\le1.4\|\mathbf w_T\|_2$）独立验收−21.9~−37.2 dB、范数恒1.40，相对弱SOCP中位改善+5.6 dB，为教师升级可行路径。

**2c 模型复杂度**（`model_complexity_v3.json`）：参数量463,106；MACs 339,148,800（batch=1，1024阵元）；FLOPs 678,297,600（2 FLOPs/MAC约定）；模型文件1.86 MB；NPU推理mean/P99为0.499/0.532 ms，CPU推理2.762 ms——资源受限平台可实时部署。

**2d 样本效率**（`stage5a_sample_efficiency_summary.json`）：固定曲面任务上4个独立教师parent即出现有效held-out改善（约+0.84 dB），6–8个parent达约+1.3 dB；D4行是相关增广不增加独立样本数。

**NPU基准明细**（256维v3，Ascend 910_9362单卡，`benchmark_v3.json`）：

| 指标 | v3 | v2（128维，对照） |
|---|---:|---:|
| 1024阵元纯推理均值 | 0.499 ms | 0.504 ms |
| 1024阵元端到端均值 | 0.632 ms | 0.658 ms |
| 纯推理P99 | 0.532 ms | 0.532 ms |
| 连续吞吐量 | 2003样本/s | 1985样本/s |
| NPU/CPU加速（mean/P99） | 5.5倍/23.1倍 | 5.4倍/25.0倍 |
| CPU/NPU最大误差（真实权重） | $2.47\times10^{-6}$ | $7.45\times10^{-8}$ |

**运行稳定性（鲁棒性指标3c）**：1000轮纯推理std=0.010 ms（CV≈2%），200轮端到端P95=0.649 ms；多次执行耗时波动极小。

### 10.3 8×8全波结果与域差异诊断

6个AI方向在全波严格门槛下为0/6通过；AI副瓣相对Taylor差0.79至2.80 dB，但与SOCP教师几乎持平（差0.3–0.9 dB）——学生保持教师水平，输给Taylor的是整个"阵因子优化类方法"家族。总接受功率约0.815至0.916，数据和接口链路工作正常。

**域差异量化**（同几何、同方向的阵因子层对照，D4归档`results_v5_d4_v2_1.json`）：阵因子层AI达−35.9 dB（竞赛目标水平）、领先Taylor 4.65–11.74 dB；进入全波层后AI损失约20.5 dB、Taylor损失约9.2 dB。该8×8几何实测曲率$\alpha\approx0.100$（恰为解析法开始退化的临界值），排除"曲率太小无优势可保"的解释——优势真实存在，是被转移过程不均匀地削弱的。

**三因素根因**（算法层结论）：①EEP多样性——阵因子模型假设阵元相同，实测同阵波束指向差30°（中心$\theta=8°$、角$\theta=38°$）、增益差约1 dB；②压副瓣机理——优化权值幅度动态范围−53 dB、依赖精确对消，对EEP差异敏感，Taylor锥削（−22 dB）从源头减能量、天然鲁棒；③小阵列放大——8×8边缘阵元占44%（1024阵元仅12%）。因此8×8结论不应双向外推至1024阵元。

修复路径：将EEP引入训练损失（EEP查表线性叠加、可微、零求解成本）或安全门控复算；阵因子安全门控不能直接替代全波安全门控。

---

## 11. 失败处理与部署原则

1. 权值导出前必须检查`safe_to_export`；
2. `rejected_after_alpha`、`degraded`和`unsupported`不得导出AI权值；
3. 支持域未知方向采用保守拒绝，不做最近点无条件外推；
4. 所有导出文件保存模型、数据、映射和权值SHA-256；
5. HFSS端口顺序必须与训练坐标顺序哈希一致；
6. 真实阵列部署应在阵因子门控之后增加EEP/S参数全波门控；
7. 任何回退都应记录模式和原因，避免只报告成功样本。

---

## 12. 当前边界

- 平面阵−35 dBc是Taylor＋LCMV系统结果（官方第一零点口径273/273），不是DeepSets单独输出；v3模型在平面阵上与Taylor持平（最差退化+0.51 dB），但不超越解析法，其价值是与曲面场景共用单一模型；
- 曲面AI目前约−24 dB，主要价值是相对Taylor改善（约6 dB）与实时性；教师验真（阶段0A）表明带范数约束的密集SOCP教师可达−21.9~−37.2 dB（独立口径），是后续质量提升的可行路径，但深曲率＋大扫描角仍受可行域限制；
- 8×8全波验证通过的是工程链路；全波层AI与SOCP均未超过Taylor，根因已量化为三因素（EEP多样性/对消敏感性/边缘占比44%），修复路径为EEP-aware训练，且小阵列结论不应外推至1024阵元（边缘占比12%）；
- 4096阵元完成质量级规模泛化验证（v4：平面/曲面×1024/4096四象限，NPU@4096实测1.242 ms），不等同于4096阵元全波验证；
- 鲁棒性为固定权值退化审计：量化退化（位置+2.17、量化+0.85、失效+4.2至+10.0 dB）与B2部分重构（+2.1至+3.0 dB，联合门槛2/960）如实呈现，尚需条件化重综合实现完整恢复；
- 频偏±10%下16/16案例中心频率附近存在合规带、3/16全带合规，退化源于半波长间距栅瓣进入可见域，属阵列设计问题。

这些边界应在答辩中主动说明，它们不会削弱已完成成果，反而证明评价过程可追溯、没有把解析指标冒充AI指标。

