# 算法报告

> 题目编号：XA-202604  
> 配套文档：《基于人工智能的大规模阵列天线综合技术报告》

---

## 1 算例说明

**基准算例条件**：

| 项目 | 条件 |
|---|---|
| 阵列形式 | 二维平面阵（32×32=1024阵元）与抛物面曲面阵（$z=\alpha(x^2+y^2)$，α∈[0.05,0.15]） |
| 阵元数量 | 1024（主验证）；4096（64×64，规模扩展） |
| 阵元坐标输入 | 三维坐标$(x_n,y_n,z_n)$，波长归一化，半波长间距，含±0.02λ随机装配扰动 |
| 扫描范围 | 上半空间±60°圆锥扫描，73个标准方向（θ=0°~60°间隔10°、φ间隔30°）+200个随机方向 |
| 相位约定 | 模型权值与HFSS端口入射波满足 $a_{HFSS}=\mathrm{conj}(w_{model})$ |

**性能指标阈值**：

| 指标 | 阈值 |
|---|---|
| 和波束副瓣（全空域） | ≤−35 dBc |
| 差波束零深 | ≤−30 dBc |
| 差波束副瓣 | ≤−20 dBc |
| 自适应置零 | ≥4点，置零深度≤−30 dBc |
| 差波束指向精度 | ≤1/30的3 dB波束宽度（RMS） |

评估采用冻结评估器1.0.0（第一零点严格口径），与任何设计网格独立。

---

## 2 数据集划分说明

### 2.1 数据集生成方式

曲面样本由SOCP切平面法逐样本离线求解生成教师权值（单样本13~23 s）：抛物面曲率α均匀采样于[0.05,0.15]，扫描方向覆盖θ=0°~60°，每样本含1024阵元坐标、Taylor坐标基线、SOCP最优权值及方向图指标。平面样本目标修正量为零（平面阵Taylor已最优），生成零成本。4096阵元样本同法生成（SOCP单样本49.6 s）。8×8专用验证数据基于真实HFSS坐标，按角度块划分并设球面保护带防泄漏。

### 2.2 样本数量与分布

| 数据集 | 样本量 | 构成 | 用途 |
|---|---:|---|---|
| 训练集 | 620 | 曲面280＋平面240＋4096阵元样本160（含60曲面教师） | 网络训练 |
| 验证集 | 30 | 1024阵元曲面 | 早停与超参选择 |
| 测试集 | 50 | 1024阵元曲面，与训练集无重叠 | 性能评估 |
| 8×8安全归档 | 544方向 | 真实HFSS坐标D4扩展 | 工程验证 |

训练数据消融验证表明：仅曲面数据训练时网络学到"扫描角→修正量"伪相关，平面阵θ≥30°退化10~16 dB；平面样本与混合规模样本为必要构成。

**扰动样本配置**（冻结案例集，固定种子，用于鲁棒性审计）：

| 扰动类型 | 配置 | 实现数 |
|---|---|---:|
| 阵元位置误差 | Δx,Δy~U[−0.05,0.05]λ | 16案例×20种子=320 |
| 幅相量化 | 0.5 dB幅度＋6 bit相位 | 16案例 |
| 阵元失效 | 5%/10%/20%（51/102/204元） | 各16案例×20种子 |
| 频率偏移 | 0.90~1.10倍频，21个频点 | 16案例×21=336 |

---

## 3 算例验证结果

### 3.1 基准场景算例结果

| 指标 | 阈值 | 实测最差值 | 通过率 | 结论 |
|---|---|---:|---|---|
| 和波束SLL | ≤−35 dBc | −35.157 dBc | 273/273 | 达标 |
| 差波束SLL | ≤−20 dBc | −21.844 dBc | 273/273 | 达标 |
| 差波束零深 | ≤−30 dBc | ≤−289 dBc | 273/273 | 达标 |
| 自适应置零（和/差） | ≤−30 dBc，≥4点 | 全部−300 dBc | 73/73 | 达标 |
| 指向精度 | RMS≤BW/30 | RMS 0.028°（门限0.195°） | 273/273 | 达标 |

AI模型同口径实测：直接输出68/73+198/200（最差−34.35 dBc）；加LCMV置零65/73+190/200（最差−34.09 dBc）。未达标方向集中于θ=60°边缘（差≤0.37 dB），由安全门控回退解析基线兜底，混合系统全指标达标。

曲面场景：AI测试集平均−24.56 dB（Taylor −21.92，教师SOCP −23.98），恢复教师改善量的128%。

### 3.2 多组扰动算例结果

**退化审计**（固定理想权值、无重综合）：

| 扰动条件 | SLL均值 | 退化量 |
|---|---:|---:|
| 位置误差±λ/20 | −32.99 dB | +2.17 dB |
| 0.5 dB+6 bit量化 | −34.31 dB | +0.85 dB |
| 5%失效 | −30.96 dB | +4.20 dB |
| 10%失效 | −28.77 dB | +6.39 dB |
| 20%失效 | −25.21 dB | +9.95 dB |

**失效权值重构**（B2活跃孔径场拟合约束最小二乘）：5%/10%/20%失效下平均恢复+2.68/+3.01/+2.09 dB（5%失效：−30.96→−33.64 dB）；联合硬指标门槛为有限恢复（2/960全过），完整恢复需条件化重综合【待验证】。

**频率偏移**（±10%）：16/16案例中心频率附近存在合规带，3/16全带合规，平均合规带宽22.6%；退化源于半波长间距栅瓣进入可见域，属阵列设计问题。

### 3.3 与传统算法对比算例

1024阵元曲面同算例，独立验收口径（201×201网格+错位+随机+峰值细化）：

| 方法 | SLL | 单场景耗时 | 加速比 |
|---|---:|---:|---:|
| Taylor（解析） | −14.2~−35.1 dB | <1 ms | 同量级 |
| 交替投影法 | −0.5~−19.3 dB | 10.8 s | 21,625× |
| 遗传算法 | −22.60 dB | 35 s | 70,140× |
| 粒子群算法 | −24.70 dB | 81.9 s | 164,128× |
| SOCP | −19.5~−22.8 dB | 13~23 s | ~45,900× |
| **DeepSets（NPU）** | **−23.3~−24.7 dB** | **0.502 ms** | — |

注：交替投影法−34.97 dB的设计网格自评结果经独立网格复核证实为采样伪影（网格间副瓣回升、权值范数失控），表中采用独立复核口径。4096阵元场景AI推理1.242 ms，相对SOCP（49.6 s）加速约40,000×。

---

## 4 程序代码说明

### 4.1 代码模块划分

| 模块 | 目录/文件 | 功能 |
|---|---|---|
| 数据生成 | `run_generate_teacher.py`、`run_multi_scan_generate.py` | SOCP教师标签生成 |
| 模型训练 | `run_deepsets_train.py`、`run_planar_fix_train.py`、`run_scale_fix_v4.py` | 网络训练与消融验证 |
| 推理 | `mylib/deepsets.py`（网络）、`mylib/official_evaluator.py`（评估器） | 前向推理与方向图评估 |
| 方向图计算评估 | `mylib/antenna_calc.py`、`mylib/evaluation.py`、`mylib/sum_diff.py` | 阵因子/和差波束/LCMV置零 |
| 鲁棒性审计 | `run_stage4a/4b*.py`、`mylib/failure_reconstruction.py` | 扰动退化与失效重构 |
| 安全导出 | D4包装器、支持域查找表、`safe_to_export`标签 | 8×8工程验证归档 |

### 4.2 依赖与运行环境

| 项目 | 配置 |
|---|---|
| 软件 | Python 3.11、PyTorch 2.7.1、torch_npu 2.7.1、CVXPY+CLARABEL、NumPy、SciPy |
| NPU | Ascend 910_9362单卡（64 GB HBM） |
| CPU | 鲲鹏40核3.0 GHz（同机A/B对照） |

### 4.3 输入输出接口

**输入**：阵元三维坐标集合（波长归一化）＋目标扫描方向$(\theta_0,\varphi_0)$＋零陷方向列表＋期望副瓣。逐阵元构造9维特征（坐标/基线权值/方向余弦/副瓣目标），特征缩放取阵元数N。

**输出**：N个复激励权值$w_n$（主瓣响应归一化），单次推理0.502 ms（1024阵元，NPU）。输出经阵因子复算与安全门控后导出；与HFSS端口对接采用复共轭映射。

### 4.4 测试用例与复现

```bash
cd project
python -m pytest tests/ -v            # 80项单元测试（含排列等变性、物理层、评估器）
python run_stage2_strict_closure.py   # 物理基线273方向冻结（~3 min）
python run_scale_fix_v4.py            # 完整训练管线（教师生成~57 min+训练~18 min）
python run_acceptance_v3_ai.py        # 模型平面验收
python run_stage4a_robustness_degradation.py   # 鲁棒性审计
python run_teacher_stage0a.py         # 教师求解器独立验真（12算例）
```

全部结果以JSON落盘（含配置、种子与哈希），可直接复核算例数值。

### 4.5 模型保存与部署

模型文件1.86 MB（463,106参数、678M FLOPs）。CPU/NPU数值一致性max_err=7.45×10⁻⁷（cos_sim=1.0）。NPU部署实测：1024阵元纯推理0.502 ms（P99 0.534 ms）、4096阵元1.242 ms、批量吞吐bs64时27,410样本/s、1000轮推理耗时std=0.010 ms（CV≈2%）。导出文件保存模型、数据映射与权值SHA-256，支持审计追溯。

---

## 5 算法局限性

1. **曲面阵列副瓣水平**：AI当前约−24 dB，距−35 dB存在差距。教师求解器专项验真（12算例独立验收+深曲率配置矩阵）表明：轻中曲率（α≤0.10）密集SOCP教师可达−27~−37 dB（升级路径可行）；深曲率（α=0.15，下垂74λ）的−22~−26 dB为该几何约束集下的凸优化数学极限（范数放宽+1 dB即饱和、零陷约束不binding、无范数约束权值发散），突破需问题表述级创新【待验证】；
2. **固定权值鲁棒性**：位置误差、幅相量化、阵元失效工况下退化如3.2节所述，B2重构为有限恢复；扰动条件化在线重综合尚未实现【待验证】；
3. **全波域差异**：8×8真实互耦环境下AI与SOCP均未超过Taylor（副瓣差0.79~2.80 dB），根因为训练目标基于理想阵因子而未含单元方向图多样性与互耦；修复路径为EEP感知训练【待验证】；
4. **规模边界**：4096阵元为阵因子级口径验证，全波验证未开展；8×8小阵列结论（边缘阵元占44%）不应外推至1024阵元（边缘占12%）场景；
5. **平面阵适用性**：规则平面阵解析法已达最优且同为亚毫秒级，系统按几何分流直接采用解析支路，AI价值定位在曲面/任意坐标场景。
