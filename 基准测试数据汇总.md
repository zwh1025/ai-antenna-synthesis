# 基准测试数据汇总

> 本文件汇总 latest upstream v3 的 Ascend 910/CPU benchmark，并列出与 fixed-task evidence 的边界。除特别说明外，所有 NPU 数字来自 `project/outputs/benchmark_v3.json`。

## 一、测试环境

| 项目 | 信息 |
|---|---|
| CPU | 华为鲲鹏 aarch64，40 核，3.0 GHz |
| NPU | Ascend 910_9362，单卡，64 GB HBM |
| 软件 | PyTorch 2.7.1、torch_npu 2.7.1、Python 3.11 |
| 模型 | upstream v3 DeepSets，256-dim，真实权重 |
| 阵列规模 | 1024 elements |
| 计时 | batch=1；纯推理 1000 iterations；端到端 200 iterations |

本 benchmark 是指定 v3 模型和硬件环境的 inference benchmark，不代表所有模型、所有阵列 geometry 或完整天线优化流程都具有相同耗时。

## 二、计算精度一致性

| 指标 | 结果 |
|---|---:|
| 最大绝对误差 | `2.47×10⁻⁶` |
| 余弦相似度 | `1.00000000` |

该结果来自相同 v3 权重和输入的 CPU/NPU 对照，说明在该测试条件下推理输出保持一致。

## 三、推理延迟

### 3.1 纯推理

输入已位于对应设备，batch=1，1000 iterations：

| 指标 | NPU | CPU |
|---|---:|---:|
| mean | `0.499 ms` | `2.762 ms` |
| P50 | `0.497 ms` | `1.674 ms` |
| P95 | `0.517 ms` | `7.107 ms` |
| P99 | `0.532 ms` | `12.296 ms` |
| 吞吐量 | `2,003/s` | `362/s` |

### 3.2 端到端

batch=1，包含 CPU 生成输入、H2D、NPU forward、D2H：

| 指标 | 结果 |
|---|---:|
| 总 mean | `0.632 ms` |
| 总 P50 | `0.626 ms` |
| 总 P95 | `0.649 ms` |
| 总 P99 | `0.664 ms` |
| 其中 H2D mean | `0.066 ms` |
| 其中 NPU inference mean | `0.523 ms` |
| 其中 D2H mean | `0.043 ms` |

## 四、批量吞吐

| batch | NPU throughput |
|---:|---:|
| 1 | `2,003/s`（连续 1000 轮口径） |
| 8 | `13,699/s` |
| 16 | `21,608/s` |
| 64 | `27,649/s` |

4096 阵元结果属于计算规模测试，不是 HFSS 或全波方向图验证。

## 五、与优化参考的计时边界

upstream v3 的历史对照记录了约 23 s 的 SOCP/cutting-plane reference generation，以及 v3 NPU 的 0.499 ms pure inference。二者可作为该固定 v3 实验口径下的 empirical comparison，但不能解释为 NPU 单独带来的硬件加速，也不是普适 SOCP acceleration factor。

另一个 fixed reconstructed curved-task study 使用 CPU online end-to-end mean `9.756 ms`，offline reference generation 约 `13.854 s/independent parent`。这组数字属于不同模型、geometry、evaluator 和数据协议，不能与本节 v3 NPU 数字直接排名。

## 六、与其他性能证据的关系

Track P 的 physics hard-spec、robustness 和 failure reconstruction 是独立的 planar physics evidence；Track AI-fixed 的 sample-efficiency/convergence 是独立的固定曲面证据。它们的结果摘要如下：

| 证据 | 结果 |
|---|---:|
| Track P Sum | 273/273；worst `-35.156576 dBc` |
| Track P Difference Azimuth/Elevation | 273/273；worst `-21.754833/-21.638634 dBc` |
| Fixed curved AI held-out improvement | 4/4；mean `1.1876 dB` |
| Fixed curved CPU online end-to-end | mean `9.756 ms` |
| Known-failure reconstruction | 5%/10%/20% recovery `2.684/3.011/2.094 dB` |

这些数字用于描述互补能力，不应合并成一个同时满足所有条件的 AI 模型结果。

## 七、复现方式

```bash
cd project
python -m pytest tests/ -v
python run_benchmark_v3.py
```

NPU benchmark 需要 Ascend 910 和相应 torch_npu 环境；无 NPU 时可运行测试和解析基线。模型权重与 teacher labels 属于环境/离线材料，不作为普通 clone 的隐式依赖。

## 八、当前结论

在指定硬件和 v3 256-dim 模型下，NPU 纯推理 mean 约 `0.499 ms`，端到端 mean 约 `0.632 ms`，并达到 batch=64 约 `27,649 samples/s`。这些是可追溯的 v3 hardware benchmark；它们不替代 Track P 的 physics hard-spec evidence，也不覆盖 fixed-task sample-efficiency study 的 CPU 模型。
