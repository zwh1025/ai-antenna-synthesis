# 基于人工智能的大规模阵列天线综合技术报告

> 题目编号：XA-202604  
> 发榜单位：中国电子科技集团公司第二十九研究所  
> 项目方向：基于人工智能的阵列天线综合

---

## 1 国内外发展调研分析情况

### 1.1 传统阵列综合方法现状与痛点

| 方法类别 | 代表算法 | 单场景耗时（1024阵元曲面，本项目实测） | 主要痛点 |
|---|---|---:|---|
| 解析法 | Taylor、Bayliss锥削 | <1 ms | 依赖规则平面栅格与可分离假设，曲面布局下性能显著退化 |
| 凸优化 | SOCP切平面 | 13~23 s | 逐场景重新求解，无法在线实时响应 |
| 启发式 | GA、PSO、交替投影（AP） | 35 s / 81.9 s / 10.8 s | 收敛慢、初值敏感；AP存在设计网格间副瓣回升问题（本项目独立网格复核实测） |

对于共形阵与快速波束调度场景，上述方法无法同时满足"曲面几何适应"与"毫秒级在线响应"的复合需求。

### 1.2 AI阵列综合研究现状与短板

深度学习阵列综合的主要路线包括CNN固定尺寸综合、LSTM变长综合与DeepSets类坐标驱动综合。现有工作普遍存在三个短板：验证域单一（仅理想阵因子层面，缺少真实互耦闭环检验）、规模受限（多数限于数百阵元）、无安全机制（网络输出直接使用，无物理校验与回退保护）。

### 1.3 本课题必要性

本课题以排列等变网络统一建模任意坐标与规模，以冻结口径评估器与真实HFSS全波数据构建从阵因子到全波的证据闭环，以物理安全门控保障工程可部署性，针对性弥补上述短板。

---

## 2 研究内容和技术路线

### 2.1 研究目标

阵列规模≥1000阵元，输入阵元三维坐标，输出逐阵元复激励权值；±60°圆锥扫描内满足五项方向图硬指标（和波束SLL≤−35 dBc、差波束零深≤−30 dBc、差波束SLL≤−20 dBc、≥4点自适应置零≤−30 dBc、指向精度RMS≤1/30波束宽度）；单次合成毫秒级并给出对传统算法实测加速比；完成四类非理想条件评估与失效权值重构。

### 2.2 主要研究内容

1. **混合综合架构**：规则平面阵采用Taylor+LCMV解析基线，曲面/任意坐标阵采用"坐标基线+DeepSets残差预测"；
2. **凸优化教师生成**：SOCP切平面离线生成高质量标签；
3. **训练数据设计**：曲面/平面/多规模样本融合，防御数据伪相关导致的分布外失效；
4. **物理安全门控**：推理输出经阵因子复算与回退机制控制导出；
5. **全波工程验证**：基于8×8曲面阵列64个复数EEP与64端口S参数的离线全波重组。

### 2.3 总体技术路线

```
阵元三维坐标 + 扫描方向 + 零陷约束
        ├─ 规则平面阵 → Taylor锥削 + LCMV自适应置零（解析支路）
        └─ 曲面/任意坐标阵 → 坐标Taylor基线 → DeepSets残差预测
                    ↓
        阵因子物理复算（主瓣/副瓣/零陷/权值范数）
                    ↓
        安全门控（AI导出 / α混合 / 解析回退 / 拒绝导出）
                    ↓
        8×8 HFSS EEP + S参数全波工程校验
```

### 2.4 约束条件与边界

| 验证层级 | 规模 | 作用 | 边界声明 |
|---|---|---|---|
| 竞赛主验证 | 32×32=1024 | 全部方向图指标直接计算验证 | 阵因子级，不含真实互耦 |
| 工程全波验证 | 8×8=64 | 权值-端口映射、EEP重组、有源匹配链路 | 不替代1024阵元指标验收 |
| 规模扩展验证 | 64×64=4096 | 跨规模泛化质量与推理基准 | 阵因子级口径 |

---

## 3 算法设计方法

### 3.1 问题建模

三维坐标阵列的远场阵因子为

$$F(\theta,\varphi)=\sum_{n=1}^{N} w_n \exp\{jk(x_nu+y_nv+z_nw)\}$$

综合问题构造为多约束优化：主瓣响应归一、副瓣区域最大响应最小化、≥4个零陷方向响应≤−30 dBc。曲面场景以SOCP切平面法离线求解作为教师标签；平面场景Taylor+LCMV提供解析解与安全回退。

### 3.2 网络架构设计

采用排列等变DeepSets网络。逐阵元输入9维特征：

$$\mathbf{x}_n=[x_n, y_n, z_n, \Re(w_{0,n}), \Im(w_{0,n}), u_0, v_0, w_0, SLL_{target}]$$

其中$w_{0,n}$为坐标Taylor基线权值。网络结构为：共享逐阵元编码器$\Phi$（3层MLP）→ 集合池化（mean+max）→ 全局解码$\rho$ → 逐阵元解码器输出复残差$\Delta w_n$，最终权值$w_n = w_{0,n} + \Delta w_n$。排列等变性保证阵元顺序置换时输出对应置换；特征缩放采用逐阵元相对幅度语义（缩放常数取阵元数N），使单一网络跨规模（1024/4096）推理语义一致。隐藏维度256，参数量463,106，模型1.86 MB。

### 3.3 数据集构建

训练集按数据构成递进设计（消融验证各构成的必要性）：

| 数据构成 | 样本量 | 作用 |
|---|---:|---|
| 曲面样本（抛物面α∈[0.05,0.15]，θ=0~60°多方向） | 280 | 教师标签主体（SOCP切平面逐样本求解） |
| 平面样本（α=0，目标ΔW=0，零生成成本） | 240 | 防御扫描角-修正量伪相关（捷径学习），消融验证：无此项时平面阵θ≥30°退化10~16 dB |
| 4096阵元样本（60曲面SOCP教师+40平面+定向增广） | 160 | 规模泛化（消融验证：无此项时4096曲面外推退化2.2~14.3 dB） |

验证集30样本（1024阵元）用于早停与超参选择；测试集50样本与训练集无重叠。8×8专用验证数据按角度块划分并设置球面保护带，防止相邻方向泄漏。

### 3.4 训练策略

损失函数为复权值残差MSE监督；验证阶段不比较MSE而是直接重算方向图指标，避免"权值误差小但方向图不合格"。优化器Adam（lr=1e-3，ReduceLROnPlateau自适应衰减），batch按规模分组（1024阵元batch=16，4096阵元batch=6），250 epochs早停。收敛特性：第100个epoch（265 s）曲面测试SLL进入最终最优值0.5 dB以内，总训练耗时751 s（本地CPU）。

### 3.5 后处理与安全门控

推理输出后逐方向复算阵因子，检查主瓣指向、副瓣电平、零陷深度与权值范数，输出四类模式：`ai`（直接导出）、`ai_alpha`（与解析基线α混合后达标）、`taylor`（解析回退）、`rejected`（拒绝导出）。安全门控保证系统输出下限等于解析基线。权值与HFSS端口入射波采用复共轭相位映射$a_{HFSS}=\mathrm{conj}(w_{model})$，已固化于全波重组程序。

---

## 4 典型结果

### 4.1 测试环境说明

| 项目 | 配置 |
|---|---|
| 仿真环境 | Python 3.11，自研冻结评估器1.0.0（第一零点严格口径，独立于设计网格） |
| NPU平台 | Ascend 910_9362单卡（64 GB HBM），PyTorch 2.7.1 + torch_npu |
| CPU对照 | 鲲鹏CPU（40核3.0 GHz），同机A/B测试 |
| 全波数据 | 8×8曲面阵列HFSS已求解工程（12.5 GHz，64个复数EEP×32580角度点，64端口S参数） |

### 4.2 阵列天线方向图性能测试结果

**和差波束与自适应置零**（73标准方向+200随机方向，官方第一零点口径）：

| 指标 | 要求 | 实测最差值 | 通过率 | 结论 |
|---|---|---:|---|---|
| 和波束SLL | ≤−35 dBc | −35.157 dBc | 273/273 | **达标** |
| 差波束SLL | ≤−20 dBc | −21.844 dBc | 273/273 | **达标** |
| 差波束零深 | ≤−30 dBc | ≤−289 dBc（数值下限） | 273/273 | **达标** |
| 和波束自适应置零（4点） | ≤−30 dBc | 全部−300 dBc | 73/73 | **达标** |
| 差波束自适应置零（4点） | ≤−30 dBc | 全部−300 dBc | 73/73 | **达标** |
| 差波束指向精度 | ≤BW/30（RMS） | RMS 0.028°（门限0.195°） | 273/273 | **达标** |

上述硬指标由Taylor+LCMV解析系统达成；AI模型在相同口径与方向集上实测：直接输出68/73+198/200（最差−34.35 dBc），加LCMV置零后65/73+190/200（最差−34.09 dBc），与解析系统性能等价；未达标方向集中于θ=60°边缘（差≤0.37 dB，解析基线自身裕量仅0.09 dB），由安全门控回退兜底，混合系统满足全部硬指标。

**曲面阵列综合能力**（1024阵元抛物面，测试集对比）：

| 方法 | 测试集平均SLL | 单场景耗时 |
|---|---:|---:|
| 坐标Taylor基线 | −21.92 dB | <1 ms |
| SOCP教师 | −23.98 dB | 13~23 s |
| GA | −22.60 dB（单算例） | 35 s |
| PSO | −24.70 dB（单算例） | 81.9 s |
| 交替投影（AP，独立网格复核口径） | −0.5~−19.3 dB（12算例） | 10.8 s |
| **DeepSets（NPU）** | **−24.56 dB** | **0.5 ms** |

曲面场景AI较Taylor改善2.6~5.7 dB，恢复教师改善量的128%，为唯一满足"曲面+亚毫秒"组合需求的方法。注：AP的−34.97 dB设计网格自评结果经独立网格复核证实为采样伪影（网格间副瓣回升15~35 dB、权值范数失控2~21倍），表中采用独立复核口径。

### 4.3 AI算法效率指标测试结果

| 指标 | 实测值 | 说明 |
|---|---:|---|
| 训练收敛 | 第100 epoch（265 s）进入最终副瓣0.5 dB内 | 总训练751 s，250 epochs早停 |
| 单次合成耗时（1024阵元） | **0.502 ms**（NPU）/ 2.762 ms（CPU） | 1000轮统计，P99=0.534 ms |
| 单次合成耗时（4096阵元） | **1.242 ms**（NPU，805样本/s） | 相对4096阵元SOCP（49.6 s）加速约40,000倍 |
| 加速比 vs AP | 21,625× | 且独立口径下AI副瓣质量优于AP |
| 加速比 vs SOCP/GA/PSO | 约45,900× / 70,140× / 164,128× | 1024阵元同算例实测 |
| 参数量 / FLOPs | 463,106 / 678M | 模型文件1.86 MB，可部署于资源受限平台 |
| 批量吞吐 | bs64时27,410样本/s | 1024阵元 |
| 样本效率 | 4个独立教师样本起有效改善（+0.84 dB），6~8个达+1.3 dB | 固定曲面任务实验 |
| 数值一致性 | CPU/NPU最大误差7.45×10⁻⁷，cos_sim=1.0 | 真实权重 |
| 运行稳定性 | 1000轮推理std=0.010 ms（CV≈2%） | 多次执行耗时波动极小 |

**规模泛化**（单一模型覆盖1024/4096双规模）：4096平面阵最差退化+0.11 dB；4096曲面阵（θ=60°）反超解析基线1.7~3.1 dB；1024阵元性能不降反升（曲面恢复率134%）。

### 4.4 鲁棒性与泛化测试结果

**非理想条件退化审计**（冻结案例集16案例×20种子，固定理想权值、无重综合）：

| 扰动条件 | 实测实现数 | 和波束SLL均值 | 退化量 |
|---|---:|---:|---:|
| 位置误差 ±λ/20 | 320 | −32.99 dB | +2.17 dB |
| 0.5 dB幅度+6 bit相位量化 | 16 | −34.31 dB | +0.85 dB |
| 5%阵元失效（51元） | 320 | −30.96 dB | +4.20 dB |
| 10%阵元失效（102元） | 320 | −28.77 dB | +6.39 dB |
| 20%阵元失效（204元） | 320 | −25.21 dB | +9.95 dB |

**失效权值重构能力**：B2失效感知重构（活跃孔径场拟合约束最小二乘）在5%/10%/20%失效下平均恢复SLL +2.68/+3.01/+2.09 dB（如5%失效：−30.96→−33.64 dB）；按全部硬指标联合门槛为有限恢复（2/960 mask全过），完整恢复需非理想条件化重综合【待实测验证】。

**频率偏移（±10%，21频点×16案例）**：16/16案例中心频率附近存在合规带，3/16全带合规，频点级联合合规76/336，平均合规带宽22.6%；退化集中于频带边缘，源于半波长间距栅瓣进入可见域，属阵列设计问题而非综合算法问题。

**8×8 HFSS全波工程验证**（真实互耦环境）：权值-端口映射、相位约定、EEP方向图重组（与HFSS直接仿真交叉验证误差10⁻⁹ dB量级）、S参数有源匹配（总接受功率81.5%~91.6%）链路全部打通；6个方向的全波层三方对比中AI与SOCP教师持平但均未超过Taylor（副瓣差0.79~2.80 dB），根因诊断为训练目标（理想阵因子）与评价域（含EEP多样性）不一致、小阵列边缘效应放大（边缘阵元占44%），修复路径为EEP感知训练【待实测验证】。

### 4.5 存在的局限与待优化点

1. 曲面阵列AI当前约−24 dB，距−35 dB仍有差距。教师求解器专项验真实验（12算例独立验收+深曲率配置矩阵+扰动孔径三臂试点）已定位：轻中曲率（α≤0.10）密集SOCP教师可达−27~−37 dB（范数受控），可支撑教师升级；深曲率（α=0.15）在当前求解口径下限为−22~−26 dB（该口径与官方第一零点评估存在已知偏差，对齐后极限待复核），配置调优已达饱和（范数放宽+1 dB、零陷约束不构成瓶颈、无范数约束权值发散）。扰动孔径方面，失效10%/20%场景SOCP教师上限较固定权值高+4.7~+6.9 dB且解析重综合无增益，为失效条件重综合（AI的主要增量场景）；
2. 固定权值非理想工况存在量化退化（见4.4），完整恢复需条件化输入重综合；
3. 全波层AI未超过解析基线，EEP感知训练为已明确的修复方向；
4. 4096阵元为阵因子级口径，4096全波验证未开展。

---

## 5 结论与后续改进建议

### 5.1 完成情况总结

| 指标类别 | 竞赛要求 | 完成情况 |
|---|---|---|
| 阵列规模 | ≥1000阵元 | 1024阵元主验证+4096规模泛化，**达标** |
| 方向图性能 | 5项硬指标 | 官方口径273/273全过，**达标** |
| AI效率 | 收敛/合成耗时/加速比/复杂度/样本效率 | 全部实测完成，单次合成0.502 ms，**达标** |
| 鲁棒性与泛化 | 位置/量化/失效/频偏/稳定性 | 退化量化+B2重构+稳定性全过，**如实呈现退化，部分达标** |
| 工程验证 | 全波/工程可行性 | 8×8 HFSS链路闭环打通，域差异已定量诊断 |

### 5.2 后续改进建议

1. **教师升级（轻中曲率）**：以带范数约束的密集SOCP替换现行教师配置——独立口径已验真轻中曲率可达−27~−37 dB（范数恒1.40受控），预期将曲面AI副瓣从−24.5 dB推进至−27~−33 dB；深曲率（α=0.15）经配置矩阵实验证实为几何物理极限（−22~−26 dB），需问题表述级创新突破；
2. **EEP感知训练**：将64个实测EEP以线性叠加形式引入训练损失或安全门控（EEP查表可微、零求解成本），修复阵因子层收益在全波层被互耦削弱的问题；
3. **失效条件重综合**：扰动孔径教师上限试点（官方口径）实测，失效10%/20%场景SOCP教师较固定权值高+4.7~+6.9 dB、且解析重综合在该场景无增益——将失效掩码作为网络条件输入实现扰动后在线权值修正，是鲁棒性指标的主要提升方向（当前B2重构为部分恢复+2.1~3.0 dB）；
4. **更大规模全波验证**：4096阵元全波验证受HFSS求解成本限制，可采用子阵分块+代理模型方式分阶段开展【待实测验证】。

本方案已形成"解析基线保底、AI残差加速、物理门控验收"的完整工程闭环：竞赛硬指标全部达标，效率指标对传统算法的实测加速达4个数量级，非理想工况与全波域差异均有量化证据与明确改进路径，具备工程可部署性与持续迭代基础。
