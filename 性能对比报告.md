# 性能对比报告：传统优化算法 vs AI快速综合

> 测试环境：华为云昇腾服务器，鲲鹏CPU(40核3.0GHz) + Ascend 910_9362 NPU(64GB HBM)
> 测试程序：`project/run_ga_pso_compare.py`
> 测试日期：2026-08-27

---

## 一、测试条件

| 项目 | 参数 |
|------|------|
| 阵列规模 | 32×32 = 1024阵元 |
| 阵列形态 | 抛物面 z = α(x²+y²)，α=0.12 |
| 扫描方向 | θ=30°, φ=0° |
| 零陷方向 | 相对主瓣：(30°,90°), (30°,180°), (30°,270°), (55°,45°) |
| 评估口径 | 3×3dB_BW 排除主瓣，41×41 uv网格 |

---

## 二、五种方法对比

| 方法 | 优化维度 | SLL (dB) | 耗时 | vs AI加速比 | 说明 |
|------|---------|---------|------|------------|------|
| Taylor(解析法) | 闭式解(64维) | -18.5 | 53ms | 43x | 曲面退化严重 |
| GA(遗传算法) | 64维幅度锥削 | -22.6 | 81秒 | 65,955x | 30种群×50代 |
| PSO(粒子群) | 64维幅度锥削 | -24.7 | 82秒 | 66,403x | 30粒子×50轮 |
| SOCP(凸优化) | 1024维复权值 | -21.8 | 23秒 | 18,644x | 15×15网格,5轮切平面 |
| **AI(DeepSets)** | **1024维残差** | **-24.2** | **1.2ms(CPU)** | **1x** | 128维网络,116K参数 |

> NPU上AI推理仅0.504ms，加速比更大：vs GA **160,000x**，vs PSO **162,000x**

---

## 三、各方法详解

### 3.1 Taylor（解析法）

- **原理**：Taylor可分离激励，闭式解，x/y方向独立设计
- **优化维度**：64（32+32幅度系数），相位由扫描方向确定
- **优点**：计算极快（微秒级），平面阵最优
- **局限**：曲面阵列无可分离闭式解，α=0.12时退化至-18.5dB（退化14dB）
- **耗时**：53ms（含方向图评估，权值计算本身<1ms）

### 3.2 GA（遗传算法）

- **原理**：种群进化，交叉+变异+选择
- **参数**：30种群 × 50代 = 1,500次评估
- **优化维度**：64维幅度锥削（在Taylor附近扰动）
- **优点**：灵活，可处理非线性约束
- **局限**：高维收敛慢，81秒仍未充分收敛；随机性强，不同种子结果差异大
- **耗时**：81.4秒

### 3.3 PSO（粒子群优化）

- **原理**：群体智能，速度+位置更新，个体最优+全局最优引导
- **参数**：30粒子 × 50轮 = 1,500次评估
- **优化维度**：64维幅度锥削
- **优点**：本测试中SLL最优（-24.7dB），收敛比GA略好
- **局限**：82秒太慢，无法在线使用；不同问题需重新调参
- **耗时**：81.9秒

### 3.4 SOCP（二阶锥规划）

- **原理**：CLARABEL内点法 + 切平面迭代（粗网格求解→密集验证→加最差点→重解）
- **参数**：15×15粗网格，5轮切平面，best-so-far保护
- **优化维度**：1024维复权值（完整自由度）
- **优点**：凸优化全局最优（给定约束下），可含零陷约束
- **局限**：23秒/阵列太慢；切平面轮数和网格密度影响精度（当前为弱基线）
- **耗时**：23秒（含切平面迭代+密集评估）

### 3.5 AI（DeepSets排列等变网络）

- **原理**：离线学习SOCP教师标签→在线前向推理输出权值残差
- **架构**：Phi(MLP编码)→mean+max池化→Rho(全局MLP)→逐阵元ΔW输出
- **参数**：128维隐藏层，116,866参数，模型473KB
- **训练**：280个SOCP教师标签，NPU训练6.5秒
- **优点**：推理1.2ms(CPU)/0.504ms(NPU)，排列等变，可变阵元数，精度与PSO相当
- **局限**：需一次性离线生成教师标签（~108分钟）；SOCP教师非全局最优，AI上限受教师质量约束
- **耗时**：1.2ms(CPU) / 0.504ms(NPU)

---

## 四、关键发现

### 4.1 AI比传统方法快4-6个数量级

| 对比 | AI耗时 | 传统方法耗时 | 加速比 |
|------|--------|------------|--------|
| AI vs Taylor | 1.2ms | 53ms | 43x |
| AI vs GA | 1.2ms | 81秒 | 65,955x |
| AI vs PSO | 1.2ms | 82秒 | 66,403x |
| AI vs SOCP | 1.2ms | 23秒 | 18,644x |
| AI(NPU) vs GA | 0.504ms | 81秒 | ~160,000x |
| AI(NPU) vs PSO | 0.504ms | 82秒 | ~162,000x |

### 4.2 AI精度与PSO相当

| 方法 | SLL (dB) | vs Taylor改善 |
|------|---------|--------------|
| Taylor | -18.5 | 基线 |
| GA | -22.6 | -4.1dB |
| PSO | -24.7 | -6.2dB |
| SOCP | -21.8 | -3.3dB |
| AI | -24.2 | -5.7dB |

AI的SLL(-24.2dB)与PSO(-24.7dB)仅差0.5dB，但速度快66,000倍。

### 4.3 不同方法的优化维度差异

| 方法 | 优化维度 | 含零陷约束 | 说明 |
|------|---------|-----------|------|
| Taylor | 64(幅度) | 否 | 闭式解，仅平面阵最优 |
| GA/PSO | 64(幅度) | 否 | 优化幅度锥削，相位固定 |
| SOCP | 1024(复权值) | 是 | 完整自由度，含零陷 |
| AI | 1024(残差) | 间接 | 学习SOCP教师的零陷约束 |

> GA/PSO优化幅度锥削(64维)比SOCP/AI优化完整复权值(1024维)自由度低，但GA/PSO的SLL反而更高，原因是GA/PSO不含零陷约束（零陷约束会恶化SLL）。这是不同优化问题的对比，时间对比是公平的，SLL对比需注意约束差异。

---

## 五、竞赛指标对应

竞赛要求："给出与解析法、交替投影、GA、PSO的加速比"

| 竞赛要求方法 | 已对比 | AI加速比 |
|------------|--------|---------|
| 解析法 | Taylor | 43x |
| GA(遗传算法) | GA | 65,955x |
| PSO(粒子群) | PSO | 66,403x |
| 交替投影(AP) | 未实现 | — |
| SOCP(凸优化，附加) | SOCP | 18,644x |

> 交替投影法(AP)未实现，但已覆盖竞赛要求的4种方法中的3种(GA/PSO/解析法)加SOCP。

---

## 六、复现方式

```bash
cd project
python run_ga_pso_compare.py    # ~3分钟(含GA+PSO)
```

> 需要先运行 `python run_generate_teacher.py` 生成教师标签(~108分钟)，AI推理需模型文件 `outputs/deepsets_model.pt`。无教师标签时跳过SOCP/AI，仅对比Taylor/GA/PSO。

---

## 七、原始数据

```json
{
  "Taylor": {"sll": -18.5, "time_ms": 53.3},
  "GA": {"sll": -22.6, "time_s": 81.4, "pop": 30, "gen": 50},
  "PSO": {"sll": -24.7, "time_s": 81.9, "swarm": 30, "iter": 50},
  "SOCP": {"sll": -21.8, "time_s": 23.0},
  "AI": {"sll": -24.2, "time_ms": 1.23}
}
```

原始JSON文件：`project/outputs/ga_pso_compare.json`
