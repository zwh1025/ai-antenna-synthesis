# 性能与效率证据报告

> 本报告按 latest upstream v3 与补充 frozen evidence 分层汇总。Track P、Track AI-v3 和 Track AI-fixed 的任务、模型、硬件与 evaluator 不完全相同，不作无条件横向排名。

## 一、证据口径

| 轨道 | 阵列与任务 | 主要指标 |
|---|---|---|
| Track P | 32×32、1024-element planar physics synthesis | official evaluator 1.0.0、Sum/Difference、adaptive null、robustness |
| Track AI-v3 | upstream v3 的 1024-element planar/curved AI workflow | 平面增广、曲面测试、Ascend 910 benchmark |
| Track AI-fixed | 一个固定 32×32、1024-element reconstructed curved geometry | held-out Taylor improvement、sample efficiency、CPU online synthesis |

## 二、Track P 物理结果

| 指标 | 冻结结果 |
|---|---:|
| Sum SLL（Taylor） | 273/273 PASS；worst `-35.156576 dBc` |
| Sum SLL（LCMV，regular 73） | 73/73 PASS；worst `-35.018982 dBc` |
| Difference Azimuth SLL | 273/273 PASS；worst `-21.754833 dBc` |
| Difference Elevation SLL | 273/273 PASS；worst `-21.638634 dBc` |
| Ideal strict adaptive null（Sum −65） | 73/73；全部 `-300 dBc` |
| Difference adaptive null（−30/−50） | 73/73；置零后 SLL worst `-21.755 dBc` |
| Position error | mean Sum-SLL degradation `+2.186 dB` |
| Amplitude/phase | mean Sum-SLL degradation `+0.860 dB` |

ideal intrinsic null 达到 numerical floor，正式展示为低于 `-100 dBc`，不把数值下限当作硬件真实性能。

## 三、upstream v3 AI/NPU 结果

### 3.1 平面/曲面验收

upstream v3 平面增广后的服务器复现结果为：40 个方向最大平面退化 `+0.34 dB`，曲面测试恢复率 `120.2%`。v3 平面验收有两套口径：

**legacy 3×3dB_BW 口径**（`acceptance_v3_ai.json`）：

| 测试臂 | 标准方向 SLL | 随机方向 SLL |
|---|---:|---:|
| Taylor+LCMV baseline | 73/73 | 195/200 |
| v3 direct | 70/73 | 199/200 |
| v3+LCMV | 69/73 | 194/200 |

**official 第一零点口径**（`results/stage2_ai_v3_official/`，与 Track P 基线同 273 方向）：

| 测试臂 | 标准方向 SLL | 随机方向 SLL | 自适应零陷 |
|---|---:|---:|---|
| v3 direct | 69/73（worst `-34.726`） | 196/200 | — |
| v3+LCMV | 61/73（worst `-34.626`） | 186/200 | 73/73（`-300 dBc`） |

第一零点口径更严（LCMV 置零引入 0.05–1.1 dB 代价，多方向基线裕量 <0.1 dB）。这些是 arm-specific results，不等同于 direct AI output 在所有方向上 100% 通过；未达标方向由安全门控回退解析基线。

### 3.2 Ascend 910 benchmark

真实 v3 权重、1024 阵元、batch=1：

| 指标 | NPU | CPU |
|---|---:|---:|
| 纯推理 mean | `0.499 ms` | `2.762 ms` |
| 纯推理 P99 | `0.532 ms` | `12.296 ms` |
| 端到端 mean | `0.632 ms` | — |
| 端到端 P99 | `0.664 ms` | — |
| 连续吞吐 batch=1 | `2,003/s` | `362/s` |
| 批量吞吐 batch=64 | `27,649/s` | — |
| CPU/NPU cosine similarity | `1.0` | — |

这些数字属于 upstream v3 256-dim 模型和指定 Ascend 910 环境。4096 阵元仅为计算规模测试，不是全波验证。

## 四、Track AI-fixed 结果

固定 reconstructed curved geometry 上的 4 个 independent test parents 均改善 coordinate-aware Taylor：

| 指标 | 冻结结果 |
|---|---:|
| AI improves Taylor | 4/4 |
| Mean / median / worst gain | `1.1876/1.0747/0.0235 dB` |
| CPU inference-only | mean `9.157 ms`；P95 `9.808 ms` |
| CPU end-to-end | mean `9.756 ms`；P95 `10.474 ms` |
| Model complexity | `597,250` parameters；`405,733,376` MACs；`811,466,752` FLOPs |

complexity 仅指 neural forward，不包含 preprocessing、方向图评估、teacher optimization 或部署开销。

## 五、Sample efficiency 与 convergence

| Independent teacher parents | D4 rows | Test improvement | Mean gain |
|---:|---:|---:|---:|
| 2 | 16 | 3/4 nominal | approximately 0 dB |
| 4 | 32 | 4/4 | approximately 0.837 dB |
| 6 | 48 | 3/4 | approximately 1.351 dB |
| 8 | 64 | 4/4 | approximately 1.326 dB |

D4 rows 是 correlated augmentation，不是 independent teacher samples。公开结论为：4 个 independent parents 开始出现 nontrivial held-out improvement，6–8 个 parents 的平均改善约 1.3 dB。严格全 validation-row `0.5 dB` teacher-proximity 未达到；best epoch、early-stop、90%-gain 和 training runtime 已记录。

## 六、Offline reference 与 online synthesis

固定 curved task 的 SOCP/cutting-plane reference generation 平均约 `13.854 s/independent parent`；训练后的 fixed-task DeepSets online end-to-end synthesis 平均约 `9.756 ms`，经验比约 `1.42×10^3`。

该比值只表示固定任务下的 offline optimization-reference generation 与 online inference 的计时比较，不是普适 optimizer acceleration claim；teacher generation 属于 offline cost，不计入 online latency。它也不能直接与 upstream v3 的 Ascend 910 数字进行性能排名。

## 七、Robustness 与 failure reconstruction

| 条件 | 冻结结果 |
|---|---:|
| Failure rate | 5%/10%/20% = 51/102/204 failed elements |
| Failure degradation | `+4.212/+6.405/+9.967 dB` Sum-SLL |
| 5% reconstruction | recovery `2.684 dB`；common-joint `2/320` |
| 10% reconstruction | recovery `3.011 dB`；common-joint `0/320` |
| 20% reconstruction | recovery `2.094 dB`；common-joint `0/320` |
| Frequency sweep | `0.90–1.10`；common-joint `76/336`；full band `3/16` |

Stage 4B 是 `C — LIMITED RECOVERY`。Azimuth Difference failure reconstruction 已运行，Elevation Difference failure reconstruction 未正式运行。上述表格是独立 frozen physics evidence，与 upstream v3 的 3 directions × 20 seeds legacy fixed-excitation 表不合并。

## 八、未纳入当前综合排名的比较

旧 v1/v2 AI、GA/PSO/AP 以及不同模型维度或不同硬件口径的历史数字，不作为当前统一 performance headline。保留它们时必须标明历史版本、任务和计时边界。SOCP 统一称为 `SOCP/cutting-plane optimization reference`，不称为普适最优性结果。

## 九、复现入口

```bash
cd project
python -m pytest tests/ -v
python run_acceptance_v3_ai.py
python run_benchmark_v3.py
python run_planar_generalization.py
```

NPU benchmark 需要 Ascend 910 环境；固定曲面 sample-efficiency 与 Track P robustness 数字来自既有 frozen evidence，不要求普通 clone 重新生成全部 teacher、mask 或 training history。
