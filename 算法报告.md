# 算法报告

> 题目编号：XA-202604
> 基于 AI 的阵列天线综合

---

## 一、具体算例验证结果

### 1.1 算例 1：1D 物理层一致性验证

**输入**：N=15–30 线阵，Taylor 25dB 激励，θ₀=30°–150°
**方法**：PyTorch 可微方向图 vs numpy 参考实现
**结果**：max_err = 2.76e-10（float64），45 样例波束指向误差 0.000°

### 1.2 算例 2：2D 和波束综合

**输入**：48×48 均匀平面阵，Taylor 35dB，θ₀=0°, φ₀=0°
**方法**：可分离 Taylor + 2×3dB_BW 排除
**结果**：SLL = -35.2 dB ✓

### 1.3 算例 3：2D 差波束综合

**输入**：48×48，Bayliss 35dB (x方向) + Taylor 35dB (y方向)
**结果**：差波束 SLL = -21.1 dB，零深 = -240 dB ✓

### 1.4 算例 4：Capon 自适应置零

**输入**：48×48 和波束 + 4 个置零方向 (30°,0°/90°/180°/270°)
**结果**：4 个零陷全部 ≤ -41.2 dB ✓

### 1.5 算例 5：扫描性能

| θ₀ | φ₀ | SLL (dB) | 排除角 (°) |
|---|---|---|---|
| 0° | 0° | -35.2 | 4.2 |
| 30° | 0° | -35.2 | 4.8 |
| 30° | 45° | -30.6 | 4.8 |
| 60° | 0° | -24.3 | 8.4 |

### 1.6 算例 6：量化鲁棒性

| 量化位数 | SLL (dB) | 退化 (dB) |
|---|---|---|
| 理想 | -34.0 | — |
| 6 bit | -34.4 | +0.4 |
| 4 bit | -33.3 | 0.7 |
| 3 bit | -32.9 | 1.2 |

### 1.7 算例 7：阵元失效

| 失效率 | SLL (dB) | 退化 (dB) |
|---|---|---|
| 0% | -34.0 | — |
| 5% | -31.0 | 3.0 |
| 10% | -28.9 | 5.1 |
| 20% | -25.2 | 8.8 |

### 1.8 算例 8：NPU 部署时延

| 平台 | 推理时延 | 加速比 |
|---|---|---|
| NPU (Ascend 910) | 48.7 ms | 617x vs GA |
| CPU | 490.3 ms | 61x vs GA |
| GA (200代) | ~30000 ms | 1x |

---

## 二、训练集、验证集与测试集

### 2.1 数据集构成

| 用途 | 来源 | 样本数 |
|---|---|---|
| 训练集 | Taylor 激励 (N=15–30, θ₀=30°–150° step3, SLL=10–30) | 13,776 |
| 验证集 | 训练中 10% 随机抽样 | ~1,378 |
| 测试集 | N=16/20/25, θ₀=45°–135°, SLL=30 | 21 |

### 2.2 数据格式

输入序列：(enc_seq, 32)，含 separator + theta0_vec + pos_vecs + padding
输出序列：(dec_seq, 32, 2)，含 separator + amp_vecs + phase_vecs + padding

### 2.3 训练参数

| 参数 | 值 |
|---|---|
| 模型 | Seq2SeqModel (13M params) |
| 主干 LSTM | [512, 512] |
| 分支 LSTM | [256, 256, 128, 128] |
| Dense 层 | [64, 32, 16] |
| Batch size | 128 |
| 学习率 | 5e-4 (ReduceLROnPlateau) |
| Epochs | 120 |
| 早停 patience | 30 |
| 损失函数 | MSE |
| 优化器 | Adam |

### 2.4 训练结果

| 指标 | 初始 | 最终 |
|---|---|---|
| Loss | 0.0132 | 0.0011 |
| Accuracy | 3.2% | 54.6% |

---

## 三、程序代码

### 3.1 代码结构

```
project/
├── mylib/
│   ├── antenna_calc.py      # 物理计算（激励/方向图/SLL/零深）
│   ├── embedding.py          # 词嵌入（numpy + torch 可微）
│   ├── models.py             # Encoder/Decoder/Seq2SeqModel
│   ├── dataset.py            # 数据集生成
│   ├── train.py              # 训练循环（NPU/GPU/CPU 自适应）
│   ├── synthesis_2d.py       # 2D 方向图综合
│   └── sum_diff.py           # 和差波束 + Capon 置零
├── tests/
│   ├── test_antenna_calc.py      # 12/12 通过
│   ├── test_embedding.py         # 7/7 通过
│   ├── test_models.py            # 7/7 通过
│   ├── test_antenna_calc_2d.py   # 8/8 通过
│   └── test_sum_diff.py          # 6/6 通过
├── run_baseline.py           # 1D 训练
├── run_2d_baseline.py        # 2D 验证
├── run_npu_train.py          # NPU 训练
├── run_full_train.py         # 全规模训练
├── run_nonideal_test.py      # 非理想验证
├── run_stage4.py             # 鲁棒性+NPU部署
└── run_2d_sum_diff.py        # 2D和差波束
```

### 3.2 运行方式

```bash
# 运行全部测试（40/40 通过）
cd project
python tests/test_antenna_calc.py
python tests/test_embedding.py
python tests/test_models.py
python tests/test_antenna_calc_2d.py
python tests/test_sum_diff.py

# NPU 训练
python run_full_train.py

# 2D 和差波束验证
python run_2d_sum_diff.py

# 鲁棒性 + NPU 部署
python run_stage4.py
```

### 3.3 环境要求

- Python 3.11
- PyTorch 2.7 + torch_npu 2.7.1
- NumPy 1.26 + SciPy 1.17
- Ascend 910 NPU（可选，CPU 也可运行）
