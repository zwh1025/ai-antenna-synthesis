# 算法报告

> 题目编号：XA-202604

---

## 一、算例验证

### 1.1 物理层一致性

- torch vs numpy: max_err = 2.76e-10
- 45样例波束指向: 0.000°误差

### 1.2 73方向正式验收

- θ=0°→1方向, θ>0°→12方向, 共73独立方向
- 3×3dB_BW口径: Taylor 100%, LCMV 100%
- 零陷: 100% (≤-38dB)

### 1.3 200随机方向

- θ∈[0,60]连续随机, φ∈[0,360)随机
- 4随机零陷, 抛物线插值指向误差
- SLL: 100%达标(3×3dB_BW), 指向RMS=0.028°

### 1.4 非理想实验(20种子×3方向)

见技术报告4.2节

### 1.5 失效补偿基准(100场景)

- 无补偿: 5%→-31.2, 10%→-28.8, 20%→-25.4
- MVDR: 全部更差(破坏Taylor锥削)
- → 传统方法无法补偿

---

## 二、数据集

### 2.1 训练数据

| 用途 | 来源 | 样本量 |
|---|---|---|
| LSTM训练 | Taylor激励(N=15-30, θ=30-150, SLL=10-30) | 13,776 |
| CNN失效补偿 | 随机失效场景 | 500(小规模) |

### 2.2 验证/测试划分

- LSTM: 10%随机验证, 监控val_loss
- CNN: 500/50/100 独立划分
- 73方向验收: 独立方向, 无重叠
- 200随机: 与73方向独立

---

## 三、代码结构

```
project/
├── mylib/                      # 核心库(6模块)
│   ├── antenna_calc.py         # 物理计算(含2D任意位置)
│   ├── embedding.py            # 词嵌入(可微)
│   ├── models.py              # LSTM seq2seq
│   ├── dataset.py              # 数据生成(含SLL, 固定编码)
│   ├── train.py                # 训练(NPU/val_loss/mask)
│   ├── sum_diff.py             # 和差波束+LCMV(修正后)
│   └── evaluation.py            # 方向自适应评估
├── tests/                      # 48/48通过
├── run_acceptance_v2.py        # 73方向验收
├── run_random_validation.py    # 200随机方向
├── run_failure_benchmark.py    # 失效基准
└── ...
```

### 运行

```bash
cd project
python tests/test_*.py          # 48测试
python run_acceptance_v2.py     # 73方向验收
python run_random_validation.py # 200随机
python run_failure_benchmark.py # 失效基准
```

### 环境

- Python 3.11, PyTorch 2.7 + torch_npu 2.7.1
- Ascend 910 NPU (2× 64GB)
- NumPy 1.26, SciPy 1.17
