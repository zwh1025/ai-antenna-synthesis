# 算法报告

> 题目编号：XA-202604。本文档面向复现，明确区分 latest upstream v3 主线与补充冻结 evidence。

## 一、环境与证据边界

upstream v3 主线使用 Python 3.11、PyTorch 2.7.1、NumPy 1.26、SciPy 1.17、CVXPY/CLARABEL，并在华为鲲鹏 CPU 与 Ascend 910_9362 NPU 上进行基准。补充 physics/fixed-task evidence 使用 official evaluator 1.0.0 和独立的 frozen study；它们与 upstream v3 的 legacy uv-domain evaluator、模型和数据划分不直接可比。

本报告中所有 AI 数字都标明所属模型和任务。`0.499/0.632 ms` 属于 upstream v3 256-dim 模型的 Ascend 910 benchmark；`9.157/9.756 ms` 属于另一个固定 reconstructed curved geometry 的 CPU fixed-task model，不能混为同一 latency。

## 二、任务与数据划分

### 2.1 Track P physics

- geometry：32×32 planar array，N=1024；
- 输入：实际阵元坐标、目标扫描方向和零陷方向；
- 扫描域：上半空间 ±60° cone；
- 方法：Taylor Sum、Bayliss Difference、LCMV adaptive null；
- evaluator：official evaluator 1.0.0；
- formal evidence：273 个 frozen target cases，另有 73-case strict-null evidence。

### 2.2 Track AI-v3

upstream v3 使用平面样本增广与曲面数据组成的训练流程，并对 1024 阵元平面验收、曲面测试和 Ascend 910 推理进行测量。`run_acceptance_v3_ai.py`、`run_benchmark_v3.py`、`run_planar_fix_train.py` 和 `run_planar_generalization.py` 是当前 v3 入口。4096 阵元只用于计算规模测试。

### 2.3 Track AI-fixed sample-efficiency study

补充 fixed-task study 只针对一个 32×32、1024-element reconstructed curved geometry。冻结 split 为 8 train parents、2 validation parents 和 4 test parents；每个 parent 的 D4 orbit 只在所属 split 内展开，属于 correlated augmentation，不是新的 independent teacher sample。

Stage 5A 使用 nested training subsets：

| Independent teacher parents | D4 rows | Seeds |
|---:|---:|---:|
| 2 | 16 | 0/1/2 |
| 4 | 32 | 0/1/2 |
| 6 | 48 | 0/1/2 |
| 8 | 64 | 0/1/2 |

每个 subset 只用自身训练 parents 计算 feature normalization 和 residual scale。checkpoint/seed 选择只使用 validation；test parents 在选择完成后才评估。

## 三、Track P 算法

### 3.1 Taylor/Bayliss

规则平面阵的 Sum 使用可分离 Taylor taper，Difference 使用 Bayliss excitation。方向图按实际二维坐标计算，主瓣、旁瓣、null 和 pointing 均由对应冻结 evaluator 评估。

### 3.2 LCMV

LCMV 以参考权值为中心：

$$
\min_w \|w-w_{\mathrm{ref}}\|_2^2
\quad\mathrm{s.t.}\quad C^H w=f .
$$

主瓣响应先按冻结规则归一化，再加入主瓣外 null constraints。理想模型的 strict null 结果不能外推到非理想或失效状态。

### 3.3 Official evaluator

Track P 的正式要求为 Sum SLL `≤ -35 dBc`、Difference SLL `≤ -20 dBc`、Difference intrinsic null `≤ -30 dBc` 和 pointing `≤ BW_sum_3dB/30`。ideal intrinsic null 达到数值精度下限时，展示为低于 `-100 dBc` 的 numerical floor。

## 四、Track AI 算法

### 4.1 Residual learning

固定曲面任务的计算链为：

```text
coordinates + target
        → coordinate-aware Taylor baseline
        → SOCP/cutting-plane optimization reference
        → residual target
        → DeepSets correction
```

DeepSets 采用 shared per-element encoder、mean/max pooling、global decoder 和 per-element complex residual head。最终权值为：

```text
w_AI = w_Taylor + Δw_AI
```

SOCP/cutting-plane 只表示在给定约束、网格和切平面设置下的 optimization reference，不作为普适最优性证明。

### 4.2 upstream v3 与 fixed-task model 的关系

upstream v3 的模型通过平面增广修复了其训练分布中的 shortcut learning，并提供 v3 平面/曲面 coverage 和 NPU benchmark。fixed-task Stage 5A 则保持 Stage 3C1 的 reconstructed curved geometry、residual representation 和 parent split 不变，用于隔离 sample count 与 convergence 的影响。二者不是同一训练协议，不能直接比较“谁优于谁”。

## 五、正式结果

### 5.1 Track P

| 指标 | 结果 |
|---|---:|
| Sum SLL（Taylor） | 273/273 PASS；worst `-35.156576 dBc` |
| Sum SLL（LCMV，regular 73） | 73/73 PASS；worst `-35.018982 dBc` |
| Azimuth Difference SLL | 273/273 PASS；worst `-21.754833 dBc` |
| Elevation Difference SLL | 273/273 PASS；worst `-21.638634 dBc` |
| Difference pointing | 273/273 PASS；worst `1.2e-6°` |
| Ideal strict adaptive null（Sum −65） | 73/73；全部 `-300 dBc` |
| Difference adaptive null（−30 / −50） | 73/73；全部 `-300 dBc`（`stage2b_diff_adaptive_closure`） |

差波束自适应零陷基线由 `capon_nulling_difference_2d`（含内在零点保护约束）作用于冻结 Bayliss 权值实现，闭环了原 baseline 的 `BASELINE_NOT_IMPLEMENTED` 项；置零后 Difference SLL 最差 `-21.755 dBc`，联合门槛（SLL ≤ −20 且 4 零陷 ≤ −30）73/73 通过。

### 5.2 upstream v3 AI/NPU

`planar_fix_v3.json` 记录服务器复现的平面 40-direction 最大退化 `+0.34 dB`，曲面测试恢复率 `120.2%`。

平面验收有两套口径，均按 arm 单独报告：

| 口径 | v3 direct（regular / random） | v3+LCMV（regular / random） |
|---|---|---|
| legacy 3×3dB_BW（`acceptance_v3_ai.json`） | `70/73`、`199/200` | `69/73`、`194/200` |
| official 第一零点（`stage2_ai_v3_official`） | `69/73`、`196/200` | `61/73`、`186/200` |

第一零点口径更严：LCMV 置零在该口径下引入 0.05–1.1 dB 代价，且多方向基线裕量本就 <0.1 dB。两套口径下 v3 均未 100% 达标；未达标方向由安全门控（AI 不劣于 Taylor 则导出，否则回退）兜底，系统级指标由 Track P 基线承担。自适应零陷在 official 口径下 73/73 全过（`-300 dBc`）。

`benchmark_v3.json` 记录真实 v3 权重在 Ascend 910 上纯推理 mean `0.499 ms`、P99 `0.532 ms`，端到端 mean `0.632 ms`、P99 `0.664 ms`，batch=64 吞吐约 `27,649 samples/s`，CPU/NPU cosine similarity `1.0`。

### 5.3 fixed-task held-out AI

在固定 reconstructed curved geometry 上，4 个 independent test parents 均改善 Taylor，mean/median/worst gain 为 `1.1876/1.0747/0.0235 dB`。这一结果只针对 held-out target directions，不是 unseen geometry 结论，也不是 Track P 的 Sum hard-spec 结果。

### 5.4 Sample efficiency and convergence

| Independent parents | D4 rows | Test improvement | Mean gain |
|---:|---:|---:|---:|
| 2 | 16 | 3/4 nominal | approximately 0 dB |
| 4 | 32 | 4/4 | approximately 0.837 dB |
| 6 | 48 | 3/4 | approximately 1.351 dB |
| 8 | 64 | 4/4 | approximately 1.326 dB |

公开 scientific conclusion 是：4 个 independent teacher parents 开始出现 nontrivial held-out improvement，6–8 个 parents 的平均改善约 1.3 dB。严格的全 validation-row `0.5 dB` teacher-proximity criterion 未达到；best epoch、early-stop、90%-gain epoch 和 training runtime 已记录。

固定曲面模型的 neural-forward complexity 为 `597,250 parameters`、`405,733,376 MACs`、`811,466,752 FLOPs`。CPU inference-only mean 为 `9.157 ms`，end-to-end mean 为 `9.756 ms`。约 `13.854 s/independent parent` 的 reference generation 是 offline optimization cost，不计入 online latency；约 `1.42×10^3` 仅为该固定任务下的 empirical ratio。

### 5.5 竞赛效率四项指标（upstream v3 口径）

- **收敛速度（2a）**：`convergence_v3.json`——v3 训练第 100 epoch（265 s）曲面 SLL 进入最终最优 0.5 dB 内，总训练 751 s（250 epochs 早停）；
- **单次合成与加速比（2b）**：`ap_compare.json`——六方法同算例：AI(NPU) 0.499 ms vs AP 10.79 s（21,625×）、SOCP 23 s（46,080×）、GA 35 s（70,140×）、PSO 81.9 s（164,128×）；Taylor 解析法同为亚毫秒级；AP 为竞赛方案明文列出的迭代算法，同算例 SLL −34.97 dB（密集网格设计与评估）；
- **模型复杂度（2c）**：`model_complexity_v3.json`——v3 参数 463,106、MACs 339,148,800、FLOPs 678,297,600、模型 1.86 MB、NPU 0.499 ms；
- **样本效率（2d）**：见 5.4（Stage 5A）。

## 六、鲁棒性与失效重构

独立 frozen physics evidence 的结果如下：

| 条件 | 结果 |
|---|---:|
| Position error `±0.05λ` | mean Sum-SLL degradation `+2.186 dB` |
| 0.5 dB amplitude + 6-bit phase | mean degradation `+0.860 dB` |
| 5%/10%/20% failure | 51/102/204 failed elements |
| Failure degradation | `+4.212/+6.405/+9.967 dB` |
| Frequency sweep | `0.90–1.10`；common-joint `76/336`；full band `3/16` |

known-mask B2 reconstruction 的平均 Sum-SLL recovery 为 5% `2.684 dB`、10% `3.011 dB`、20% `2.094 dB`；960 个 mask 的 common-joint hard-spec pass 为 `2/960`。这属于 limited recovery。Azimuth Difference 已运行，Elevation Difference failure reconstruction 未正式运行。

## 七、复现入口

### upstream v3

```bash
pip install -r requirements.txt
cd project
python -m pytest tests/ -v
python run_acceptance_v3_ai.py              # legacy 3x3dB_BW 口径
python run_stage2_ai_v3_official.py         # official 第一零点口径
python run_stage2b_diff_adaptive_closure.py # 差波束自适应零陷闭环
python run_benchmark_v3.py                  # 需 Ascend 910
python run_planar_generalization.py
```

NPU benchmark 需要 Ascend 910 环境；训练和 teacher generation 属于离线步骤，模型与 labels 不作为普通 clone 的隐式依赖。

### frozen evidence

`results/stage2_strict_closure/`（Track P 全量 273 方向基线）、`results/stage2_ai_v3_official/`（v3 三臂官方口径）、`results/stage2b_diff_adaptive_closure/`（差波束零陷闭环）为已落盘正式结果。固定曲面 sample-efficiency、Stage 4 robustness 结果应优先读取相应冻结 evidence summary，不要求普通用户重新生成全部 teacher、mask 或训练 history。历史实验文件不应被当作最新 v3 结果。

## 八、最终边界

- upstream v3 的 NPU 结果是指定 v3 256-dim 模型和 Ascend 910 环境的真实 benchmark；
- fixed-task sample-efficiency 只适用于一个 reconstructed curved geometry 和冻结 parent split；
- 本地 Track P hard-spec closure 不归因于 DeepSets direct output；
- 4096 是计算规模测试，不是全波验证；
- 不声称跨 geometry 的 unseen-task generalization；
- 不声称严格 `0.5 dB` teacher-proximity 已达到；
- 不声称 known-failure reconstruction 完全恢复理想 hard specs；
- 不把 SOCP/cutting-plane reference 称为普适最优性结果。
