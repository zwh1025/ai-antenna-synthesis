# 算法报告

> 题目编号：XA-202604

---

## 一、算例验证

### 1.1 物理层一致性

- torch vs numpy: max_err = 2.76e-10
- 45 样例波束指向: 0.000 度误差
- 批量 vs 单样本: max_err = 0.00e+00

### 1.2 73 方向正式验收

- theta=0 度 1 方向, theta>0 度 72 方向, 共 73 独立方向
- 3x3dB_BW 口径: Taylor 100%, LCMV 100%
- 零陷: 100% (<=-38dB)
- 指向精度: RMS 0.028 度 (要求 <=0.195 度)

### 1.3 200 随机方向

- theta 属于 [0,60] 连续随机, phi 属于 [0,360) 随机
- 4 随机零陷, 抛物线插值指向误差
- SLL: 100% 达标 (3x3dB_BW), 指向 RMS=0.028 度

### 1.4 非理想实验 (20 种子 x 3 方向)

| 条件 | SLL 均值 | SLL 最差 | 退化 |
|---|---|---|---|
| 理想 | -35.5 dB | -35.2 dB | 0 |
| 0.5dB+6bit 量化 | -34.5 dB | -32.1 dB | +3.1 |
| +/-lambda/20 扰动 | -33.4 dB | -30.4 dB | +4.7 |
| 5% 失效 | -31.5 dB | -29.4 dB | +5.8 |
| 20% 失效 | -25.5 dB | -23.1 dB | +12.1 |
| +/-10% 频偏 | -20.3 dB | -0.9 dB | +34.3 |

### 1.5 失效补偿基准 (SOCP 负结果)

- 无补偿: 5% -> -31.5, 10% -> -28.7, 20% -> -25.5
- SOCP 切平面: Delta=+0.0 dB (Taylor 已最优)
- 结论: 失效退化来自物理孔径缺口, 非权值次优

### 1.6 曲面阵列 SOCP 验证 (正结果)

| 曲率 alpha | Taylor | SOCP | 改善 |
|---|---|---|---|
| 0.10 | -20.8 dB | -24.9 dB | -4.2 dB |
| 0.15 | -17.1 dB | -21.7 dB | -4.6 dB |

### 1.7 DeepSets AI 综合结果

| 数据集 | Taylor | SOCP | AI | 恢复率 |
|---|---|---|---|---|
| v1 验证集 | -19.70 dB | -23.11 dB | -23.51 dB | 111.5% |
| v1 测试集 | -20.18 dB | -23.27 dB | -23.72 dB | 114.6% |
| v2 验证集 | -21.65 dB | -24.14 dB | -23.87 dB | 89.3% |
| v2 测试集 | -21.92 dB | -23.98 dB | -23.62 dB | 82.4% |

### 1.8 NPU 基准验证

| 指标 | NPU | CPU | 加速比 |
|---|---|---|---|
| 训练 (128 维, batch16) | 2.42 ms/epoch | 38.03 ms/epoch | 15.7x |
| 训练 (512 维, batch64) | 15.10 ms/epoch | 1300.45 ms/epoch | 86.1x |
| 推理 P50 (1000 轮) | 0.502 ms | 1.332 ms | — |
| 推理 P99 (1000 轮) | 0.532 ms | 13.272 ms | 25.0x |
| 连续吞吐量 | 1985/s | 367/s | 9.0x |
| 精度一致性 | max_err=7.45e-08 | — | cos_sim=1.0 |

---

## 二、数据集

### 2.1 SOCP 教师标签

| 用途 | 来源 | 样本量 | 划分 |
|---|---|---|---|
| v1 单方向训练 | SOCP 切平面 (alpha=0.08-0.15, theta=30) | 280 | 200/30/50 |
| v2 多方向训练 | SOCP 切平面 (alpha=0.08-0.15, theta=0/15/30/45/60) | 280 | 200/30/50 |
| 总计 | 560 个 SOCP 教师标签 | 560 | — |

每个标签包含: 阵元坐标 (px,py,pz)、Taylor 基线权值 (归一化)、SOCP 最优权值、SLL 指标、扫描参数。

### 2.2 验证/测试划分

- v1/v2: 严格独立划分 (200 训练 / 30 验证 / 50 测试)
- 73 方向验收: 独立方向, 无重叠
- 200 随机方向: 与 73 方向独立
- 排列等变性测试: 随机打乱阵元顺序

---

## 三、代码结构

```
project/
|-- mylib/                      # 核心库 (8 模块)
|   |-- antenna_calc.py         # 物理计算 (激励/方向图/SLL/2D任意位置)
|   |-- deepsets.py             # DeepSets 排列等变网络
|   |-- embedding.py            # 词嵌入 (numpy + torch 可微)
|   |-- models.py               # LSTM seq2seq
|   |-- dataset.py              # 数据生成 (含SLL, 固定编码)
|   |-- train.py                # 训练 (NPU/val_loss/padding mask/best checkpoint)
|   |-- synthesis_2d.py         # 2D 综合 (梯度下降, 备用)
|   |-- sum_diff.py             # 和差波束 + LCMV
|   `-- evaluation.py           # uv 域评估器 (-30dB 连通域 + 3x3dB_BW)
|-- tests/                      # 61 项测试 (8 文件)
|   |-- test_antenna_calc.py    # 12 项
|   |-- test_antenna_calc_2d.py # 8 项
|   |-- test_deepsets.py        # 9 项 (含排列等变性)
|   |-- test_embedding.py       # 7 项
|   |-- test_models.py          # 7 项
|   |-- test_train.py           # 8 项
|   |-- test_sum_diff.py        # 6 项
|   `-- test_failure_mask.py    # 4 项
|-- run_acceptance_v2.py        # 73 方向验收
|-- run_random_validation.py    # 200 随机方向
|-- run_nonideal_v2.py          # 非理想实验
|-- run_curved_verify.py        # 曲面阵列 SOCP 验证
|-- run_cylindrical_verify.py   # 圆柱面阵列验证
|-- run_curved_nonideal.py      # 曲面+量化/失效联合
|-- run_generate_teacher.py     # SOCP 教师标签生成 (v1)
|-- run_multi_scan_generate.py   # 多方向教师标签生成 (v2)
|-- run_deepsets_train.py       # DeepSets 训练 + 三方对比
|-- run_benchmark.py            # CPU/NPU 标准基准
|-- run_benchmark_supplement.py # 端到端+长轮次推理
|-- run_bounded_socp.py         # SOCP 失效补偿验证 (负结果)
|-- run_nonuniform_verify.py     # 非均匀坐标验证 (负结果)
|-- run_socp_32.py              # 32x32 SOCP 基础
|-- run_socp_cutting.py         # SOCP 切平面
`-- run_failure_benchmark.py    # 失效补偿基准
```

### 运行

```bash
cd project
python -m pytest tests/ -v         # 61 项测试
python run_acceptance_v2.py        # 73 方向验收 (~6min)
python run_curved_verify.py        # 曲面 SOCP 验证 (~11min)
python run_generate_teacher.py     # 教师标签生成 (~108min)
python run_deepsets_train.py       # DeepSets 训练 (~2min NPU)
python run_benchmark.py            # NPU/CPU 基准 (~10min)
```

### 环境

- Python 3.11, PyTorch 2.7.1 + torch_npu 2.7.1
- Ascend 910_9362 NPU (64GB HBM), 鲲鹏 CPU (40 核 3.0GHz)
- NumPy 1.26, SciPy 1.17, CVXPY 1.9 + CLARABEL

---

## 四、算法核心

### 4.1 解析基线

Taylor 可分离激励: W_2d(m,n) = W_x(m) x W_y(n), 闭式解, 计算量极小。
LCMV 自适应置零: min ||w-w_ref||^2 s.t. C^H w = f, 归一化 w_ref 使主瓣响应=1。

### 4.2 SOCP 教师

min_{w,t} t s.t. a_main^H w = 1, |a_i^H w| <= t, |a_null^H w| <= epsilon
CLARABEL 内点法 + 切平面迭代 (粗网格求解 -> 密集网格验证 -> 加入最差副瓣点 -> 重新求解)。

### 4.3 DeepSets 排列等变网络

- Phi (逐阵元编码): 输入 (x,y,z, w_re,w_im, u0,v0,w0, SLL) -> MLP -> 隐藏特征
- 池化: mean + max over N (排列不变)
- Rho (全局网络): MLP -> 全局特征
- 输出: concat(元素特征, 全局特征) -> MLP -> DeltaW (实部, 虚部)
- 最终权值: W = W_baseline + DeltaW_AI
- 损失: MSE(DeltaW_AI, DeltaW_SOCP)

### 4.4 NPU 部署

- 自动设备选择: NPU > GPU > CPU (get_device())
- 全算子 NPU 兼容 (Linear/ReLU/mean/max/cat)
- float64 物理损失在 CPU 侧计算
- 模型 0.47-1.9MB, 可部署到边缘 NPU
