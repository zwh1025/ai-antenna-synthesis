# 项目交接文档

> 供新对话窗口使用，快速了解项目全貌和后续工作

## 一、项目概况

**题目**：挑战杯 XA-202604，基于 AI 的阵列天线综合技术研究
**发榜单位**：中国电子科技集团公司第二十九研究所
**提交截止**：2026-09-15
**GitHub**：https://github.com/zwh1025/ai-antenna-synthesis
**环境**：PyTorch 2.7 + torch_npu (Ascend 910 NPU) / CPU / GPU 均可运行

## 二、已完成的工作

### 解析基线（100% 达标）
- 32×32（1024 阵元）Taylor 可分离激励 + LCMV 置零
- 3×3dB_BW 口径：和波束 SLL ≤ -35 dBc，73 方向 + 200 随机 100% 达标
- LCMV 4 个零陷 ≤ -38 dB，Δ‖w‖ = 0.003（几乎不改变 Taylor）
- 差波束 SLL ≤ -21 dB，零深 ≤ -38 dB

### 非理想实验
- 0.5dB+6bit 量化：退化 +3.1 dB（可接受）
- ±λ/20 位置扰动：退化 +4.7 dB
- 5%/10%/20% 阵元失效：退化 +5.8/+9.1/+12.1 dB
- ±10% 频偏：退化 +34 dB（波束偏斜，需真时延）

### AI 方向探索（负结果有价值）
- 阵元失效补偿：SOCP 无法改善 Taylor（Δ=0.0 dB），5 种 L2 方法全部失败
- 非均匀坐标(±λ/20)：SOCP 无改善，坐标 Taylor 已最优
- 以上两个方向已停止，负结果写入论文

### 曲面阵列（正结果，AI 有价值）
- 抛物面 z = α(x²+y²)，α=0.10/0.15
- Taylor 退化 14-18 dB（无闭式解），SOCP 改善 4.2-4.6 dB
- SOCP 13s/阵列 → AI 目标 50ms（260 倍加速）
- **这是下一步 AI 训练的方向**

### 关键 Bug 修正记录（7 项）
1. 相位公式 `linspace→k·cos(θ)·pos`
2. accuracy `dim=2→dim=32`（虚假 97%→真实 5%）
3. `masked_mse_loss` `.any→.sum==(0)`（loss 恒 0→正确）
4. LCMV 主瓣约束 `f[0]=1→归一化w_ref`（SLL -11→-35 dB）
5. 主瓣排除 `2×3dB→3×3dB`（60°扫描 -24→-35 dB）
6. 评估器相位符号 `+phase→-phase`（指向误差 30°→0.07°）
7. `~active_idx` 整数误用→`w[fmask]=0`（无补偿 SLL -6→-31 dB）

## 三、代码结构

```
project/
├── mylib/                  # 核心库
│   ├── antenna_calc.py      # 物理计算（激励/方向图/SLL/2D任意位置）
│   ├── embedding.py          # 词嵌入（numpy + torch 可微）
│   ├── models.py             # LSTM seq2seq（已验证但未收敛）
│   ├── dataset.py            # 数据生成（含SLL输入，固定[-1,1]编码）
│   ├── train.py              # 训练循环（NPU/val_loss/padding mask/best checkpoint）
│   ├── synthesis_2d.py       # 2D 综合（梯度下降，备用）
│   ├── sum_diff.py            # 和差波束 + LCMV（已修正）
│   └── evaluation.py         # uv域评估器（-30dB连通域 + 3×3dB_BW）
├── tests/                  # 52 项测试全部通过
├── run_2d_sum_diff.py      # 48×48 和差波束验证
├── run_acceptance_v2.py    # 73 方向正式验收
├── run_random_validation.py # 200 随机方向
├── run_nonideal_v2.py      # 非理想实验
├── run_curved_verify.py    # 曲面阵列 SOCP 验证（正结果）
├── run_bounded_socp.py     # SOCP 失效补偿验证（负结果）
├── run_nonuniform_verify.py # 非均匀坐标验证（负结果）
├── run_socp_32.py          # 32×32 SOCP 基础
├── run_socp_cutting.py     # SOCP 切平面
└── run_failure_benchmark.py # 失效补偿基准
```

## 四、后续工作（按优先级）

### 第一优先：DeepSets AI 训练（核心 AI 成果）

**目标**：学习曲面阵列坐标 → SOCP 最优权值的映射，50ms 近似 13s 的 SOCP

**步骤**：
1. 生成 200 个曲面几何（α=0.08-0.15 随机），每个跑 SOCP 切平面保存权值作为标签
2. 设计 DeepSets 网络：
   - 逐阵元输入：(x, y, z, 基线权值实部, 虚部, u0, v0, w0, SLL)
   - 逐阵元 MLP 编码 → 全阵列 mean+max 池化 → 全局特征回传 → 输出 ΔW(实部,虚部)
   - 最终权值：W = W_baseline + ΔW_AI
3. 训练/验证/测试按不同曲率划分（200/30/50）
4. 排列等变性测试：打乱阵元顺序，输出对应打乱，方向图不变
5. 三方法对比：无补偿 vs SOCP vs AI

**退出条件**：AI 恢复 SOCP 改善量的 80% 以上

### 第二优先：提升 SOCP 教师

- 当前粗网格 15×15，5 轮切平面，SLL=-25 dB
- 改 25×25 网格，10 轮，可能提升到 -30 dB
- 多扫描方向（θ=0°/30°/60°）曲面验证

### 第三优先：扩展场景

- 圆柱面阵列（z = R·cos(x/R)）
- 曲面 + 量化联合
- 曲面 + 5% 失效联合（AI 可能在这个联合场景增量更大）

### 论文和提交物

| 提交物 | 状态 | 备注 |
|---|---|---|
| 技术报告 | ✅ 已扩写 | `技术报告.md`，6 章 + 15 参考文献 |
| 算法报告 | ✅ 已完成 | `算法报告.md` |
| PPT | ⏳ 大纲完成 | `PPT大纲.md`，需转成实际 PPT |
| 视频 | ❌ 未做 | 需录屏运行结果 |
| 程序代码 | ✅ 已完成 | 52/52 测试通过 |
| 项目计划 | ✅ 已完成 | `项目计划方案.md` |

## 五、关键约束和注意事项

1. **相位约定**：统一弧度，`psi = k*x - phase`，方向图用 `conj(w)·exp(j·psi)`
2. **主瓣排除**：用 3×3dB_BW 口径（竞赛标准），不单独用严格第一零点
3. **失效索引**：用布尔 `w[fmask]=0`，禁用 `~active_idx`（整数按位取反 Bug）
4. **SOCP 求解器**：用 CLARABEL，不用 SCS（大规模不收敛）
5. **NPU 限制**：不支持 float64 的 logsumexp/masked_fill，物理损失在 CPU 计算
6. **评估器**：`evaluate_uv` 在 uv 均匀网格上计算，返回 `sll_3bw`（竞赛口径）和 `sll_connected`（严格口径）
7. **conda 装包**：用 `conda install` 不用 `pip install`（避免装错环境）

## 六、给新对话窗口的嘱咐

> 你将接手一个阵列天线 AI 综合项目。项目已完成解析基线（Taylor+LCMV，竞赛指标 100% 达标），并系统验证了 AI 在均匀平面阵下无增量（负结果）。下一步是曲面阵列的 DeepSets AI 训练。
>
> 关键文件：
> - `技术报告.md`：完整论文，先读这个了解全貌
> - `README.md`：项目概览和当前状态
> - `CONTRIBUTING.md`：环境配置和运行方式
> - `project/mylib/antenna_calc.py`：物理计算核心
> - `project/mylib/sum_diff.py`：和差波束 + LCMV
> - `project/mylib/evaluation.py`：uv 域评估器
> - `project/run_curved_verify.py`：曲面阵列 SOCP 验证（正结果，下一步 AI 的基础）
>
> 第一步：读 `技术报告.md` 了解项目全貌
> 第二步：运行 `python tests/test_antenna_calc.py` 验证环境
> 第三步：运行 `python run_curved_verify.py` 看曲面 SOCP 结果
> 第四步：开始实现 DeepSets 网络训练
>
> 重要约束：不要重启失效补偿或非均匀坐标 AI 方向（已验证为负结果）。只做曲面阵列 AI。不要修改已通过测试的物理层和 LCMV 代码。新代码只加不改旧代码。
